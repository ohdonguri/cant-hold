# 이번 웨이브는 못 막습니다 — 설계 문서

성급 합성 타워디펜스. 모바일 세로, 한 판 6~8분.

---

## 1. 코어 루프

0. 판 시작 전 **타워 7종 중 3종을 고른다**. 고른 종류만 나온다
1. **빈 칸을 눌러 그 자리에 1성 타워를 소환한다.** 종류도 자리도 고른다
2. 같은 종류·같은 성 2개 드래그 → 합성, 성급업. **웨이브 중에도 된다**
3. 3성 / 5성 진입 시 **분기 2택**, 7성 진입 시 공통 특성 3개 중 1택
4. **첫 웨이브는 눌러야 시작한다.** 이후로는 자동 진행이고 "빨리 보내기"로 대기시간을 건너뛴다(보상 없음)
5. 웨이브 30 클리어 or 관문 파괴 → 메타 통화로 영구 언락

**핵심 긴장**: 3웨이브 앞까지 적 편성이 예고됨. 지금 최적인 빌드가 3웨이브 뒤에 무력화됨.
→ "지금 5성 만들까, 아껴뒀다 다음 편성 보고 만들까"가 매 판 반복되는 결정.

---

## 2. 공통 규칙

### 덱

판 시작 전에 타워 7종 중 **3종을 플레이어가 고른다**(`DECK_SIZE`). 고른 종류만 소환된다.

7종 전부를 랜덤 소환하면 같은 종류가 모이지 않아 5성이 영원히 안 나온다 —
시뮬레이션에서 23웨이브까지 84회 소환에 종류당 12개, 5성에 필요한 16개에 못 미쳤다.

**크기 3은 "고르게 한다"와 한 몸이다.** 덱 간 편차(신호)와 같은 덱 반복 시의
흔들림(노이즈, 1.76)을 비교하면:

| 덱 크기 | 신호 | 최고−최저 | 판정 |
|---|---|---|---|
| 3종 | **2.70** | **11.9웨이브** | 진짜 결정 |
| 4종 | 1.63 | 5.8 | 애매 |
| 5종 | 1.24 | 4.1 | 노이즈에 묻힘 |

5종은 어느 두 덱이든 최소 3종이 겹쳐서(71%) 덱 정체성이 안 생긴다. 고르게 해도
뭘 골랐는지가 결과에 안 묻는 **가짜 결정**이 된다. 반대로 3종을 랜덤으로 주면
시작부터 진 판이 나오므로 불공평하다. 그래서 3종 + 선택이다.

적 저항 수치를 올려 덱 차이를 벌리려는 시도는 실패했다(중갑 방어 40→90,
마갑 마방 0.45→0.78, 면역몹 증량 → 편차 1.13→1.27). 덱 크기 말고는 안 움직인다.

3종이라 구멍이 4개다. 어떤 조합이든 적 7종을 다 커버하지는 못하며, **무엇을 포기할지가
이 판의 첫 결정**이다. 중갑은 마방이 0이라 마법 타워가, 마갑은 방어력이 낮아 물리 타워가
자동으로 답이 되므로 완전히 막히는 조합은 없다.

### 성급

| 성급 | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
|---|---|---|---|---|---|---|---|
| 데미지 계수 | 1.0 | 2.4 | 5.8 | 13.8 | 33.2 | 79.6 | 191 |

- 합성: 같은 종류·같은 성 2개 → 다음 성. 종류 다르면 불가. 비용 `8 x 합성 전 성급`
- **합성은 웨이브 중에도 된다.** 판이 끝난 뒤(`over`·`clear`)에만 막힌다 — 거기서는 보드가
  판정을 마쳤다. 오래 준비 단계 전용이었고, 근거는 「결과 타워가 자리를 옮기면 오라 범위와
  마력로 조준선이 한복판에서 통째로 흔들린다」였다. **그 현상은 지금도 맞다. 뒤집은 이유는
  이 줄의 다음 문장이 이미 적고 있던 것이다 — 소환과 판매는 웨이브 중에도 된다.** 새 타워가
  한복판에 생기는 것도 범위와 조준선을 똑같이 바꾼다. 즉 「전투 중에 판이 흔들린다」는 이미
  이 게임의 규칙이었고 합성만 유일한 예외였다. 게다가 자리 이동은 2x2 결과에서만 나고(1x1 은
  `mergeSpot` 이 부모 자리 중 하나를 고른다), 2x2 는 §5성 자리를 플레이어가 고른다 이후로
  **플레이어가 직접 고르므로** 「모르는 새 옮겨진다」가 아니다
- 분기 선택: 3성 2택 → 5성 2택(3성 분기에 종속) → 7성 공통특성 3랜덤 중 1택
- 타워당 최종 4빌드. 7종 = **28빌드**

배수가 2 근처면 합성해도 총 화력이 그대로다(2개가 1개가 되므로). 적 HP는 지수로
오르는데 화력이 소환 횟수에 비례해 선형으로만 오르면 게임이 성립하지 않는다.
그래서 배수를 2보다 확실히 크게 잡아 합성 자체가 화력 증가가 되도록 했다.

### 스테이지

**맵 하나로 모든 요구를 만족시킬 수 없다.** 경로를 촘촘하게 깔면 2x2 주머니가 사라지고,
주머니를 남기면 경로가 규칙적이 되어 어느 칸에 놓든 똑같아진다. 그래서 맵을 타협하는 대신
**진행 콘텐츠로 만들었다.** 클리어하면 다음이 열린다(진행도는 localStorage).

| # | 이름 | 레인 | 웨이브 | 경로 | 배치 | 2x2 | 커버 편차 | 성격 |
|---|---|---|---|---|---|---|---|---|
| 1 | 외곽 도로 | 1 | 20 | 34칸 / 35 | 24 | 10 | 0.82 | 어디에 놓아도 비슷 — 규칙만 익히는 판 |
| 2 | 이중 병목 | 1 | 25 | 35칸 / 37 | 27 | 13 | **1.87** | 좋은 자리가 명확히 갈림 |
| 3 | 갈래길 | 2 | 30 | 31칸 / 23+23 | 24 | 10 | 1.37 | 적이 반씩 나뉨 |
| 4 | 역류 | 2 | 30 | 35칸 / 37+37 | **32** | 13 | **2.14** | 2번 맵을 양쪽 끝에서 |
| 5 | 분수령 | 2 | 30 | 44칸 / 31+31 | **48** | **27** | 1.79 | 보드가 9x14 — 놓을 자리가 두 배 |
| 6 | 합수 | 2 | 30 | 56칸 / 34+36 | **53** | **31** | **1.99** | 보드가 10x14 · **입구가 둘** |

**⑥ 은 ⑤ 와 뼈대가 닮았고 머리가 다르다.** ⑤ 는 입구가 하나라 한 무리가 갈라졌다
다시 합치는데, ⑥ 은 **입구가 둘**이라 애초에 따로 들어온 둘이 바닥에서 만나기만 한다.
그 차이가 숫자로 나오는 자리가 아래 「전레인동시」다 — ⑤ 는 공유하는 머리 구간 덕에
개방 행의 37.5% 가 두 갈래를 다 덮는데 ⑥ 은 24.5% 다. **위쪽은 갈래마다 따로 답해야
하고** 한 대로 둘을 보는 자리는 아래 합수 지점 근처로 몰린다.

**좌표를 `forked(10,14)` 생성값에서 가져오지 않았다.** `tools/paths.js` 의 「갈래 NxM」은
같은 패턴을 크기만 바꿔 찍는 것이라 **「갈래 9x14」가 곧 ⑤ 분수령이다**(경로 44 · 길이
31 · 배치 48 · 전레인동시 18/48 로 한 자리도 안 다르다). 그걸 그대로 10x14 로 쓰면
⑤ 를 한 칸 넓힌 복사판이 된다.

4스테이지는 지형이 2스테이지와 같지만 `openRows` 가 6 이 아니라 8 이다. 그래서
배치 칸과 편차가 2스테이지와 다르다 — 이 표가 한동안 2스테이지 값(27 / 1.87)을
그대로 달고 있었다.

5스테이지는 **처음으로 7x10 을 벗어난 판**이다. 아래 「보드를 넓히는 것」이 그
근거이고, 좌표는 `tools/paths.js` 의 `forked(9,14)` 생성값 그대로다.

**커버 편차**가 핵심 지표다. 배치 칸마다 사거리 안에 들어오는 경로 칸 수의 편차인데,
0 에 가까우면 어디에 놓든 똑같다는 뜻이라 배치가 결정이 아니게 된다.
1스테이지가 0.82인 것은 의도적이다 — 처음부터 자리 싸움을 시키지 않는다.

**배치 칸과 편차는 개방 행 기준이다.** 판이 시작할 때 놓을 수 있는 칸이 개방 행뿐이라
「첫 배치가 결정인가」는 그 범위에서 재야 한다. 행이 다 열린 뒤로 재면 값이 커진다
(2스테이지 1.87 → 2.23). `tools/paths.js` 는 `1.87/2.23` 처럼 둘 다 찍는다.

후보 맵 비교는 `node tools/paths.js`. 나선처럼 그럴듯해 보이는 형태는
실제로 재보면 배치 16칸에 2x2 0곳으로 게임이 성립하지 않는다.

#### 판을 몇 장까지 붙일 수 있나 — 상한은 지형이 아니라 선택 화면이다

카드 목록은 판 수로 나눠 자동 축소되는데 **하한에 걸리면 더 안 줄고 그 아래
(이어하기·로그인)가 통째로 화면 밖으로 밀려난다.** 다섯 장이 658px 에서 꼬리 바닥
정확히 658 로 여유가 0 이었고, 여섯 장이면 **742 로 84px 넘쳤다.** 도전 판을 여섯째
카드로 안 붙이고 탭으로 가른 것(#39)이 이 실측 때문이었는데, 그건 **레이아웃 티켓을
피한 것이지 없앤 게 아니었다** — 본편이 여섯 장이 되면서 그대로 돌아왔다.

**답은 셋째 줄(`●●●○○○ 웨이브 30 갈래 2`)을 좁은 화면에서 접는 것이다.** 헤더를
줄이는 쪽은 최대 32px 이라 84 를 혼자 못 만들고, 목록 스크롤은 이어하기·로그인의
좌표를 카드 목록에서 따는 지금 구조를 통째로 바꾼다. 접는 쪽은 카드당 20px 씩
벌면서 잃는 것이 가장 적다 — **계단에서 몇 번째인지는 카드 번호가 이미 말한다.**
설명 줄은 못 접는다(잠긴 카드에서 「이전 스테이지를 깨야 열린다」가 그 줄이다).

**접는 조건은 「화면이 몇 px 이하」가 아니라 「세 줄로 깔면 꼬리가 화면 밖인가」다.**
그래서 지금 들어가는 화면에서는 한 줄도 안 뺏긴다 — 다섯 장 658px 은 여유가 0 인
채로 여전히 세 줄이다.

실측(390 폭 · 카드 높이 / 로그인 줄 바닥):

| 장수 | 844px | 742px | 700px | 667px | 658px |
|---|---|---|---|---|---|
| 6 | 83.7 세줄 812 | 72.0 세줄 742 | 59.7 접힘 668 | 54.2 접힘 635 | 52.7 접힘 626 |
| 7 | 72.0 세줄 826 | 55.4 접힘 710 | 51.0 접힘 679 | **12px 넘침** | **21px 넘침** |
| 8 | 59.8 접힘 812 | 51.0 접힘 742 | **42px 넘침** | **75px 넘침** | **84px 넘침** |

**접기로 번 것은 판 하나치다.** 일곱 장이 되는 날 667·658 이 다시 걸리고, 그때는
남은 둘(스크롤 · 헤더 축소)을 써야 한다. `tools/test.js` 의 기기 목록
`[844, 667, 658]` 은 **그대로 유지한다** — 통과시키려고 기기를 빼는 것은 회귀를
숨기는 것이다.

**레인이 여러 개면 갈래길과 역류가 같은 구조로 처리된다.** 적은 스폰될 때 레인을
번갈아 배정받고, 각자 자기 레인의 길이만큼 이동한다.

**HP 배율은 직관과 반대로 간다.** 레인이 갈리면 타워가 한쪽만 커버하므로 실효 화력이
반토막 난다. 그래서 3·4스테이지는 배율이 1 미만(0.6 / 0.8)인데도 더 어렵다.
선택 화면에 배율을 그대로 노출하지 않고 난이도 점으로 표시하는 이유다.

**5스테이지는 4스테이지와 같은 0.8 이다.** 같은 2레인이라 위 문단이 똑같이 걸린다.
**이 값은 시뮬이 고른 게 아니다** — 1.0 / 0.8 / 0.7 / 0.6 을 각각 재 봤는데 난이도
지표가 전부 같았다(클리어 0/8 · 5성 8/8 · 중앙 w28~30). 배율을 40% 움직여도 그리디의
판정이 안 갈리므로 이 범위에서 시뮬은 눈금 노릇을 못 한다. 그래서 **이웃 판과 같은
값으로 두고 넘긴다**가 근거다. 한때 0.6 으로 내려 둔 적이 있는데 그건 아래
「조기 전멸 꼬리」를 상쇄하려던 것이었고, **꼬리가 0.6 에서도 남는 것이 실측으로
드러나 되돌렸다** — 시뮬 결함을 출시 상수로 상쇄하려던 것이라 방향 자체가 틀렸다.
이 판을 고른 진짜 근거인 커버 편차 1.79 를 그리디가 오래 **아예 못 쟀다** — 균등
난수로 놓았기 때문이다. **#35 가 그 자를 붙였다.** 그리디는 이제 후보 칸에서 두 곳을
뽑아 커버가 큰 쪽에 놓고(`tools/sim.js` `SUMMON_SAMPLES = 2`), 고른 자리의 커버
백분위가 0.51 → 0.67 로 올라왔다(`node tools/place.js` · 시드 12345 · 6시행).
편차가 큰 판일수록 그 선택이 이득이므로 **이 판을 고른 근거가 비로소 시뮬에 반영된다.**

**그래도 `hpMult` 는 안 건드렸다.** 위 「배율을 40% 움직여도 판정이 안 갈린다」는
다시 안 쟀고, 배치가 좋아진 뒤의 배율 재측정은 난이도 재조정 티켓이 할 일이다.
바뀐 것은 아래 「밸런스는 시뮬레이션으로 역산한다」의 스테이지 표에서 ⑤ 가 전체평균
22.6 · 클리어덱 0/35 로 **다섯 판 중 가장 어려운 판으로 찍힌다(⑥ 이 붙기 전 기준)**는 사실이다(35덱 x 6회).

`tools/tune.js` 의 실배포 행을 스테이지만 바꿔 재면(시드 12345 · 5시행 · 클리어 99):

| 기준판 | 사망웨이브(중앙값) | 진도 p (5시행·랜덤덱) | 최고성급 |
|---|---|---|---|
| 스테이지3 (tune 기본) | 29 | 0.93 | 7 |
| 스테이지4 | 29 | 0.94 | 7 |
| **스테이지5 분수령** | **27** | **0.85** | 7 |

(#48 뒤에 다시 뜬 값이다. 세대별로 S3/S4/S5 사망웨이브가 균등 난수 배치 27 / 27 / 20
→ #35 뒤 29 / 30 / 22 → 지금 29 / 29 / 27 이다. **`p` 는 5시행 평균이고 사망웨이브는
중앙값이라 서로 나눠떨어지지 않는다** — 두 열은 다른 통계량이다.)

> **⑤ 의 22 → 27 을 「#48 이 판을 쉽게 만들었다」로 읽지 마라.** 5시행 중앙값이라
> 표본이 `20,30,30,20,27` 로 두 덩어리에 걸쳐 있어 중앙값이 한 칸만 움직여도 20 과
> 30 사이를 오간다. 같은 판을 **210판**으로 재는 `npm run curve` 는 ⑤ 의 `p` 를
> 0.75 → 0.75 로 **한 자리도 안 움직인 것**으로 찍는다. 아래 「열 이름에 표본을 박은
> 이유」가 말하는 바로 그 자리다 — 표본이 다르면 같은 정의라도 다른 수가 나온다.

**열 이름에 표본을 박은 이유가 있다.** `p` 의 *정의*는 하나뿐이고(아래 §난이도의 눈금 ·
도구 셋이 전부 평균을 쓴다) 그건 이제 안 갈린다. 하지만 **같은 정의라도 표본이 다르면
값이 다르다** — 위 스테이지3 의 0.93 은 `tune` 이 **랜덤 덱으로 5판** 재서 나온 값이고,
아래 눈금 표의 0.91 은 `curve` 가 **35덱 전수로 210판** 재서 나온 값이다. 둘 다 맞고
둘 다 「진도 p」인데 수가 다르다. 그래서 **표본을 안 적은 `p` 는 인용하지 마라.**
이 티켓(#37)이 처음 열린 이유가 정확히 이것이었다 — 고정덱 8판의 `8/8` 과 35덱의
`17/35` 를 나란히 놓고 읽은 것.

> **폐기:** `index.html:166` 의 ⑤ 분수령 주석이 「목표 밴드 밖」이라며 이 표를
> 가리키는데, **그 밴드 열은 #37 에서 지웠다**(절대 웨이브로 적혀 있어 판을 옮기면
> 뜻이 달라지는 자였다 — `tools/tune.js` 머리 참고). 그 주석이 가리킬 표는 이제
> 없으니 찾지 마라. `index.html` 이 동결이라 주석 자체는 못 고쳤고, 그 파일을
> 여는 다음 티켓이 「진도 `p`」로 바꾸면 된다.

**⑥ 이 붙기 전까지 그리디를 실제로 막는 판은 5스테이지 하나였다.** 다만 `tune.js` 의 `STAGE` 는
2 로 두었다 — 기준판을 옮기는 것은 이 표 하나로 할 일이 아니다(위 「계단」 문단과 같은
이유). 위 표는 `STAGE` 만 바꿔 일회성으로 잰 값이다.

**5스테이지의 계단은 오래 아무도 안 재고 있었다.** 옛 게이트 「뒤 스테이지가 더
어렵다」가 `rows[0]`(1스테이지)과 마지막 행 **둘만** 비교해서 4→5 사이는 안 봤다.
#37 이 그 게이트를 「첫 판이 가장 쉽고 마지막 판이 가장 어렵다」로 바꾸면서 마지막
판을 **모든 판과** 견주게 됐으므로 이제 4→5 도 본다. 다만 인접 쌍 자체(3↔4 같은)는
여전히 `known()` 으로 찍기만 한다 — 아래 「계단의 3 대 4」와 같은 이유다.

#### 레인을 늘리면 정말 규칙이 바뀌는가 — 「전레인동시」

**「출구가 여럿이라 여러 곳을 막아야 한다」는 실측과 어긋난다.** 9열 판에서 출구
셋의 최대 간격은 4칸인데 사거리 2.2 짜리 타워는 ±2 를 덮으므로, **「출구마다 한
대」는 9폭에서 수학적으로 강제할 수 없다.** 3레인 후보 F 의 출구 `(0,13) (4,13)
(8,13)` 은 한 대로는 못 덮지만 `(1,12) (6,13)` **두 대**면 전부 덮인다.

레인을 늘려 실제로 바뀌는 것은 출구가 아니라 **중간 구간**이고, 그걸 재는 자가
`node tools/paths.js` 의 **「전레인동시」** 열이다 — 개방 행 배치 칸 중 사거리 안에
**모든 레인**의 칸이 들어오는 칸 수:

| 판 | 보드 | 레인 | 전레인동시 (개방 행) | |
|---|---|---|---|---|
| ③ 갈래길 | 7x10 | 2 | 14 / 24 | **58.3%** |
| ⑤ 분수령 | 9x14 | 2 | 18 / 48 | **37.5%** |
| **3레인 후보 F** | 9x14 | **3** | **1 / 43** | **2.3%** |

⑤ 는 레인이 갈렸는데도 자리 셋 중 하나꼴로 「갈래를 다 덮는 자리」가 있어서
「어디를 막을까」가 1레인 판에 가깝다. F 는 그런 자리가 사실상 없으므로 갈래마다
따로 답해야 한다. **남은 1곳은 `(4,6)` 이고 세 레인 모두에 거리가 정확히 2.00 이라,
사거리가 중앙값(2.2) 미만인 타워는 거기 서도 못 덮는다.**

> **주의 — 「0곳」으로 적힌 기획 실측은 계산 오류였다.** 개방 행을 `y >= openRows`
> (9x14 에서 행 8~13, 6줄)로 걸었는데 실제 개방 행은 `y >= h - openRows`(행 6~13,
> 8줄)다. 빠진 행 6~7 안에 위 `(4,6)` 이 있다. 같은 오류로 분모도 전부 작았다
> (⑤ 18/34 · ③ 10/14 로 적혀 있었다). 위 표는 `tools/paths.js` 가 원래 쓰던
> 개방 행 정의(`free(h - open)`)로 다시 잰 값이다.

**후보 셋을 재서 F 를 골랐다.**

| 후보 | 레인 길이 | 전레인동시 | 기각 사유 |
|---|---|---|---|
| A 삼거리 | 21/20/18 | 2 / 38 | 편차는 1.78 로 가장 크지만 **한 자리가 셋을 다 덮는 곳이 더 많다** — 2레인 판과 같아지므로 편차를 산 대가가 헛돈다 |
| G 세 변 | 18/21/**11** | 2 / 48 | 3번 레인이 나머지의 **절반**이라 「짧은 레인부터」가 늘 정답이 되어 선택이 사라진다 |
| **F (채택)** | **18/19/17** | **1 / 43** | 레인 길이가 균등하고 전레인동시가 사실상 없다 |

**F 의 편차 1.48 은 `paths.js` 권고선(1.5) 아래다.** 그래도 된다 — 그 줄은 게이트가
아니라 출력이고(`npm test` 에 이 판정을 읽는 단언이 없다), ① 외곽 도로 0.82 ·
③ 갈래길 1.37 이 이미 아래인 채로 출시돼 있다.

#### 전레인동시에는 **바닥**이 있다 — 낮을수록 좋은 값이 아니다

위 절은 이 값이 낮을수록 「갈래마다 따로 답한다」에 가까워진다고만 적었다. **틀렸다.
그 아래에 그리디의 벽이 있고, 거기를 넘으면 `npm test` 의 밸런스 게이트가 통째로
무너진다.** #44 에서 3레인 판(2.3%)을 본편으로 올리려다 이 벽을 정면으로 맞았고,
같은 티켓에서 2레인 후보 열몇 개를 재며 벽의 위치를 처음으로 쟀다.

무시드 200판 · 밸런스 고정덱(`파쇄+마력+조폐`) · `hpMult 0.8` · 조기 전멸은 `w<10`:

| 후보 | 전레인동시 | 조기 전멸 |
|---|---|---|
| R1a 엇갈려나감 | 45.7% | 3.0% |
| **⑥ 합수 (채택)** | **24.5%** | **2.5%** |
| 갈래10x14 (생성 후보) | 32.7% | 8.0% |
| LB+RC 자기목 | 29.5% | 21.0% |
| S2 늦은합수 | 20.0% | 29.5% |
| R2 두 가장자리 | 8.8% | 45.5% |
| R5 합류끊기 | 12.1% | 74.0% |
| C2 세 갈래 (3레인) | 2.3% | **게이트 전면 실패** |

**비율만으로는 안 갈린다 — 채택안이 24.5% 에서 2.5% 인 것이 그 증거다.** 무엇이 두
갈래를 같이 덮느냐가 중요하다. **합수 지점처럼 경로가 실제로 겹치는 곳**이 그 역할을
하면 낮은 비율에서도 초반 화력이 모이고, 좌우로 갈라 놓기만 하면 같은 비율에서
무너진다. 「합류를 끊는다」로 만든 후보가 74% 로 최악이었다.

**원인은 판이 아니라 그리디다.** 자리를 「사거리 안 경로 칸 수」로만 고르므로
(`tools/sim.js` `SUMMON_SAMPLES = 2`) 갈래마다 따로 답해야 하는 판에서 초반 화력을
못 모은다. 사람은 갈래를 보고 놓으므로 이 벽은 시뮬 쪽 성질이다 — §꼬리를 재는 자의
「원인은 판이 아니라 균등 난수 배치였다」와 같은 자리이고, **고칠 곳도 같다**(그리디를
레인 인지하게 만드는 것. 별도 티켓이다 — 고치면 S1~S5 의 `curve` 값이 같이 움직인다).

#### 계단 게이트와 5성 게이트는 서로 반대를 요구한다 — ⑥ 이 그 사이에 낀다

**마지막 판을 새로 붙일 때 처음 드러난 구조다.** `npm test` 의 세 게이트 중 둘이
같은 축(그리디가 얼마나 나아가는가)을 **반대 방향으로** 잡아당긴다.

| 게이트 | 요구 | 방향 |
|---|---|---|
| 첫 판이 가장 쉽고 마지막 판이 가장 어렵다 | 이 판의 `p` 가 ⑤ 보다 **낮다** | 덜 나아가야 한다 |
| 5성 도달률 (8판 중 7판) | 그리디가 5성을 만들 만큼 **오래 산다** | 더 나아가야 한다 |
| 어느 스테이지도 초반 전멸은 없다 | 8판 중 둘 이상이 일찍 죽지 않는다 | 더 나아가야 한다 |

**⑤ 가 통과하는 것은 중앙 사망이 w27 이라 거의 안 죽어서다**(단독 200블록 1/200 = 0.5%).
계단을 지키려면 ⑥ 의 중앙은 w20~24 여야 하고, 그 깊이에서는 「8판 중 둘이 일찍 죽는」
블록이 **2~4%** 로 남는다. 실측(단독 200블록 · 25런 밸런스블록):

| `hpMult` | 210판 `p` | 게이트 실패 | 계단 실패 | 계단 여유(최소) |
|---|---|---|---|---|
| 0.8 | 0.719 | ~1% | 2/25 | −0.021 |
| 0.9 | 0.646 | 3~6% | 2/25 | −0.096 |
| 0.95 | 0.647 | 3% | 0/25 | +0.042 |
| **1.0 (출시)** | 0.616 | **4%** | **0/25** | **+0.100~+0.121** |
| 1.1 | — | 8% | 0/25 | +0.158 |

**지형으로도 안 닫힌다.** 전레인동시를 24.5% → 28.3% 로 올려도 3% → 2.5% 로만 움직였고,
29.6% 인 다른 지형은 오히려 14.5% 였다 — 이 축의 지배 변수가 아니다.

##### 그래서 계단을 풀었다 — 「가장 어렵다」는 애초에 요구가 아니었다

**유저가 계단 쪽을 풀기로 정했다.** 근거는 이 티켓을 열 때 유저가 한 말이다:

> 계단을 더 올리는 게 막 그렇게 난이도를 많이 올릴 필요는 없다는 의미였어
> **비슷해도 되고** 더 어려워도 돼

즉 **「마지막 판이 가장 어렵다」는 게이트가 요구보다 엄격했던 것**이고, 위 표의 막다른
골목은 그 엄격함이 만든 것이었다. 게이트를 **「뒤로 갈수록 안 쉬워진다」(동률 허용)** 로
바꾸고 나니 ⑥ 을 억지로 누를 이유가 사라져 `hpMult` 를 1.0 → **0.85** 로 되돌릴 수 있었고,
밸런스 게이트 실패가 **4% → 0/40** 이 됐다. **배율로 초록칠한 것이 아니다** — 계단이
강요를 안 하니 배율이 원래 자리로 돌아온 것이다.

**푼 것은 동률까지다.** 허용 오차 `STAIR_EPS = 0.10` 은 실측이다 — 8판 무시드에서 「같은
난이도인데 뒤가 더 쉽게 나온」 최대 폭이 S3↔S4(포화) +0.040 · S5↔S6(hpMult 0.80) +0.046 ·
S5↔S6(0.85) **+0.063** 이었다. **포화된 쌍으로 정하면 안 된다** — S3↔S4 는 둘 다 1.00 에
눌려 흔들림이 작다. 잡아야 할 진짜 신호는 한 자리 크다(① 1.00 대 ⑥ 0.70 = 0.30).

뮤테이션으로 확인한 것(각 8런):

| 뒤집기 | 계단 게이트 | 스위트 전체 |
|---|---|---|
| ⑤ 를 ⑥ 보다 어렵게 (⑤ `hpMult` 1.6) | **FAIL** `S5→S6 +0.44` | FAIL |
| ③ 을 어렵게 · ④ 를 쉽게 | **FAIL** `S3→S4 +0.53` | FAIL |
| ⑥ 을 ⑤ 보다 쉽게 (⑥ `hpMult` 0.4) | 7/8 | **8/8** |

**⑥ 을 쉽게 만드는 뒤집기만 계단 게이트가 한 번 놓쳤다.** `p` 가 1.00 에서 포화라
⑤(0.90)와의 간격이 최대 0.10 밖에 안 벌어져 허용 오차와 같은 자리이기 때문이다.
그 자리는 **「마지막 스테이지는 안 깨진다」(clears ≤ 2)가 8/8 로 잡는다** — 판이 쉬워지면
`p` 보다 클리어 수가 먼저 움직인다. 두 줄이 같은 사건을 다른 칸으로 보는 셈이라,
동률 허용이 「아무거나 통과」가 되지는 않는다.

**남은 차이(⑤ 0.5% 대 ⑥ 0~2%)의 원인은 그리디가 레인을 안 보는 것이다.** 고칠 곳은
`tools/sim.js` 이고 별도 티켓이다.

> **「고치면 S1~S5 곡선이 같이 움직인다」는 자동이 아니다.** 이 줄과 위 §전레인동시
> 문단이 오래 그렇게 적고 있었는데, #48 이 그리디의 배치 자를 **종류별로** 갈아
> 끼우고도 진도 `p` 를 여섯 판 전부 ±0.01 안에 두었다(§판이 어느 타워를 좋아하는가).
> 배치를 만지면 곡선이 움직일 **수** 있다는 경고로 읽고, 움직였는지는 매번 재라.

#### 3레인은 기각됐다 (#44)

`tools/paths.js` 가 3레인 후보 둘을 **이미 두 번 기각해 두었다**(`✗ 경로 짧음` ·
`✗ 경로 짧음, 배치가 무의미`). 그걸 안 읽고 3레인 판을 본편 6번째로 올리려 했고,
아래가 그때 실측한 것이다. 다음 사람이 같은 데를 또 파지 않게 남긴다.

**웨이브·`hpMult` 어느 축으로도 안 됐다.** 210판 진도 `p` 는 밴드에 넣을 수 있었지만
(`hpMult` 0.4~0.2), 밸런스 게이트가 **모든 값에서** 빨간불이었다:

| `hpMult` | 210판 `p` | 초반 전멸 실패 | 5성 도달률 실패 |
|---|---|---|---|
| 0.6 | 0.346 | 11/12 | 12/12 |
| 0.4 | 0.514 | 19/20 | 17/20 |
| 0.2 | 0.740 | 18/20 | 4/20 |
| 0.1 | — | 7/8 | 3/8 |

**`hpMult` 로는 원리적으로 못 넘는다 — 누수 피해가 적 HP 와 무관하기 때문이다**
(`state.life -= d.leak`). 한 갈래가 비면 매 웨이브 3분의 1이 그냥 걸어 들어오고,
HP 를 10분의 1로 줄여도 관문 20 은 똑같이 깎인다. 그리디가 한 갈래를 통째로 안 덮은
채 끝나는 판이 27~35% 나왔다(⑤ 는 0/40).

**#42 가 출시한 설정(`waves 15` · `hpMult 0.6`) 그대로도 못 넘는다.** 도전 판일 때는
밸런스 블록이 `curveStages()` 로 걸러서 **이 두 자를 한 번도 안 댄 판**이었다.

### 보드를 넓히는 것 (9x14 로 갔다 — 5스테이지)

스테이지가 4개뿐이라 판을 더 만들려 한다. 보드 크기는 이미 스테이지별이다
(`loadStage()` 가 `CFG.BOARD_W`/`BOARD_H` 를 갈아끼운다) — 넓히는 데 엔진 작업은 없다.

`tools/paths.js` 로 통로 간격 3행 뱀(1레인)과 갈래(2레인)를 크기별로 재 봤다.

| 형태 | 보드 | 배치 | 2x2 | 커버 편차 | 레인당 길이 |
|---|---|---|---|---|---|
| 뱀 | 8x12 | 35 | 12 | 1.37 | 41 |
| 뱀 | 10x14 | 45 | 16 | 0.77 | 60 |
| 뱀 | 12x16 | 66 | 30 | 0.64 | 83 |
| **갈래** | **9x14** | **48** | **27** | **1.79** | 31 |
| 갈래 | 10x14 | 55 | 32 | 1.79 | 33 |
| 갈래 | 12x16 | 89 | 59 | 1.58 | 39 |

**1레인은 넓히면 판이 죽는다.** 편차가 1.37 → 0.77 → 0.64 로 무너진다. 규칙적인 통로를
넓히면 커버가 평준화돼서 1스테이지(0.82)로 수렴한다 — 이 문서 첫 문단의 충돌이
크기를 키운다고 풀리지 않는다는 뜻이다.

**2레인은 버틴다.** 1.58~1.79 를 유지한다. 레인이 갈리면 타워가 한쪽만 커버하므로
자리값이 살아남는다. 다만 12x16 에서 1.58 로 떨어지기 시작한다 — 넓히기에도 상한이 있다.

**9x14 갈래로 갔다**(5스테이지 「분수령」). 오래 10x14 를 후보로 적어 뒀는데 그쪽으로
안 갔다. 두 후보의 **편차는 1.79 로 같고**, 10x14 의 유일한 우위는 배치 칸이다
(55 대 48 — 2스테이지의 2.04배 대 1.78배). 그런데 배치 칸이 남는 것은 이 절의
「덱 크기는 여기서 못 정한다」가 못박아 둔, **지금 쓸 데가 없는 이점**이다.
반면 칸 크기는 즉시 손해다:

| 열 수 | 7 (현행) | 8 | **9** | 10 |
|---|---|---|---|---|
| 셀(390 폭) | 52.9px | 46.3px | **41.1px** | 37.0px |

**가로가 병목이다.** `resize()` 는 `min((w-20)/BOARD_W, (h-100-150)/BOARD_H)` 인데
9x14 에서 세로는 `594/14 = 42.4` 로 가로(41.1)보다 크다 — 즉 **`HUD_TOP`/`HUD_BOTTOM`
을 줄여도 넓은 보드의 칸은 1px 도 안 커진다.** 칸을 정하는 것은 열 수와 `padX` 10
뿐이다. 이 줄이 없어서 「HUD 를 줄여 공간을 벌자」가 두 번 나왔다.

41.1px 은 현행의 78%다. 셀에 비례하는 것(스프라이트 `cell*0.82`, ★배지
`round(cell*0.26)` = 10.7px, 드래그 원 `cell*0.42`)이 같이 줄어들므로 여기는
**스크린샷이 진짜 게이트다**(`npm run shot` 의 `9-wideboard` 컷). 헤드리스는 못 잡는다.
11x16 부터는 배치가 80칸으로 뛰는데 편차는 오히려 떨어져서 볼 것도 없다.

**조기 전멸 꼬리 — 넓은 보드의 대가.** 그리디 시뮬을 돌리면 9x14 에서만 `w<10`
전멸이 나온다. **판당 1000판**으로 다시 뜬 실측(밸런스 덱 `파쇄+마력+조폐` · 무시드):

| 판 | 보드 | hpMult | `w<10` (k=1) | `w<10` (k=2, 현행) | 5성 미달 |
|---|---|---|---|---|---|
| ③ 갈래길 | 7x10 | 0.6 | 0/1000 | 0/1000 | 0/1000 |
| ④ 역류 | 7x10 | 0.8 | 0/1000 | 0/1000 | 0/1000 |
| **⑤ 분수령** | **9x14** | **0.8** | **17/1000 (1.7%)** | **6/1000 (0.6%)** | 같은 판 |
| ⑤ 분수령 | 9x14 | 0.8 | (k=3 참고) | 2/1000 (0.2%) | 같은 판 |

**`w<10` 과 「5성 미달」은 매번 같은 판이다** — 초반에 죽으면 5성을 만들 시간이 없다.
그래서 `npm test` 의 두 게이트가 같은 사건 하나에 반응한다. 둘은 여전히 **별개
게이트**이고 보는 필드도 다르다(사망 웨이브 / 최고 성급) — 맞춘 것은 **견디는 표본
수(`NEED`)를 같은 값으로 둔 것**뿐이다. 기준이 갈리면 같은 꼬리를 재면서 한쪽만
통과하는 상태가 생긴다.

원인은 판이 아니라 **그리디가 타워를 균등 난수로 놓던 것**이었다 — 9x14 는 경로에서
먼 빈 칸의 비율이 7x10 보다 훨씬 커서, 초반 두세 대가 통째로 헛자리에 서면 그대로
무너진다. 사람은 경로 옆에 놓으므로 이 꼬리는 시뮬 쪽 성질이다. **같은 배율에서
7x10 이 0/1000 인 것이 근거다.**

**#35 가 배치를 고쳐 꼬리를 2.8배 줄였다**(1.7% → 0.6%, `SUMMON_SAMPLES = 2`).
**다만 0 이 되지는 않았다 — 그렇게 적지 않는다.** 200판으로 재면 0/200 이 나오는데
그건 0.6% 를 200판으로 보는 것이라 30% 확률로 그렇게 나올 뿐이고, 「없앴다」의
근거로 쓰면 안 된다. 남은 0.6% 는 `k` 를 올리면 더 줄지만(k=3 에서 0.2%) 그쪽은
그리디를 더 잘하게 만들어 난이도 눈금을 밀어내므로 안 갔다. 아래 「꼬리를 재는 자」가
그 거래를 숫자로 적는다.

**hpMult 로 이걸 상쇄하려 하지 마라 — 한 번 해 보고 되돌렸다.** 0.6 은 꼬리를
8배 줄이지만 없애지는 못하고(25런에 `S5:3` 이 한 번 나왔다), 그 대가로 출시 상수가
4스테이지보다 낮아진다. **시뮬 결함을 게임 상수로 덮는 거래**라 방향이 틀렸다.
고칠 자리는 `tools/sim.js` 의 배치 규칙이었고, #35 가 거기를 고쳤다.

**덱 크기는 여기서 못 정한다.** 배치 칸이 2배라는 건 타워를 더 들고 있을 수 있다는
뜻까지다. `DECK_SIZE` 3 의 근거는 소환 횟수(84소환 / 7종 = 종당 12개 < 5성에 필요한 16개)라
골드가 정하는 문제고, `tools/tune.js` 가 재야 한다. 그런데 그 시뮬은 오래 3·4스테이지를
0/35 클리어로 내는 반면 사람은 7성으로 깼다 — **7성 이후의 플레이를 재현하지 못한다.**
그 격차의 한 조각이 #31 이었다(그리디가 덱의 셋째 종류를 아예 안 지었다). 고친 뒤
3·4스테이지 클리어덱이 6/35 · 2/35 로 올라왔지만 아직 사람 쪽이 한참 위다.
덱을 늘리는 판단은 나머지를 마저 고친 뒤에 한다.

### 꼬리를 재는 자 — 최솟값 게이트는 판을 안 잰다

이 꼬리는 `npm test` 의 두 게이트를 같이 건드렸고, **둘 다 8표본의 최솟값을 보고
있었다.** 무시드 8판에서 최솟값은 「판이 망가졌는가」가 아니라 「시뮬이 한 번 나쁘게
굴렀는가」에 반응한다. 그래서 자를 바꿨다 — 임계는 그대로 두고 **이상치 한 판을
견디게** 했다.

| 게이트 | 전 | 후 | 전에 실질적으로 요구하던 것 |
|---|---|---|---|
| 어느 스테이지도 초반 전멸은 없다 | `w[0] >= min(10, 중앙값)` | `w[1] >= …` | 8판 전부 |
| 5성 도달률 | `five / 8 >= 0.9` | `five >= 7` | **8판 전부** (7/8 = 87.5% < 90%) |

**5성 쪽은 「90%」라고 적혀 있었지만 실제로 동작한 값은 100% 였다.** `n = 8` 에서
90% 를 넘는 정수는 8 뿐이라, 한 판만 무너져도 빨간불이었다. 이름만 비율이고 실제로는
최솟값 게이트다 — 다음 사람이 「90% 인데 왜 한 판도 못 놓치나」에서 막히는 자리라
`tools/test.js` 주석에 같이 적어 뒀다.

**한 칸 물러선 것은 맞다.** 7/8 = 87.5% 는 옛 표기 90% 보다 낮다. 다만 낮춘 것은
임계가 아니라 **표본 하나를 견디게 한 것**이고, 옛 값이 강제하던 100% 는 무시드
8표본으로는 지킬 수 없는 기준이었다. 두 게이트가 같은 꼬리를 보므로 자를 하나
(`NEED = n - 1`)로 묶어 기준이 갈리지 않게 했다.

**이건 「시드를 박는 것」과 다르다.** 시드는 표본을 하나로 고정해 눈금을 통과로
굳히지만(같은 블록이 문서로 금지한다), 통계량 교체는 8표본을 그대로 두고 어느
순서통계량을 읽을지만 바꾼다 — 분포가 실제로 나빠지면 여전히 빨간불이 된다.

`node tools/test.js` 30런 실측(hpMult 0.8): **실패 1런**(27% → 3.3%). 스테이지 행
155개 중 8/8 이 147 · 7/8 이 6(새 자가 흡수) · **6/8 이 2**. 유일한 실패런은 S5 에서
**두 판이 같이 무너진 것**(`S5:4/5` · 5성 6/8)이라 두 게이트가 같이 걸렸다 — 설계대로
동작한 것이지 자가 헐거워서 새어 나온 게 아니다.

**#35 뒤 32런 재실측: 실패 0런.** S5 는 32런 전부 클리어 0/8 이고 초반 전멸도 없다.
다만 **「0/32 라서 없어졌다」로 읽으면 안 된다** — 게이트가 걸릴 확률이 런당 0.098%
이면 32런에 한 번이라도 볼 확률이 3.1% 라, 0/32 는 기대되는 결과이지 증거가 아니다.
꼬리는 얇아진 것이지 사라진 게 아니다(위 표).

**남은 3.3% 는 자의 문제가 아니라 꼬리가 두꺼운 탓이었다.** 이걸 0 에 가깝게 만드는
길은 **꼬리 자체를 얇게 하는 것**뿐이다. 견디는 이상치를 2판으로 늘리면 8판 중 6판만
봐서 게이트가 사실상 무의미해지고, hpMult 를 내리는 건 위 문단이 이미 기각했다.

**#35 가 그 꼬리를 얇게 했다.** `tools/sim.js` 의 배치를 균등 난수에서 **k-표본
최고**로 바꿨다 — 후보 칸에서 `k` 곳을 뽑아 커버(사거리 안 경로 칸 수)가 큰 데 놓는다.
`k` 를 고르는 규칙은 목적함수가 아니라 **제약 + 최소성**이다. 판당 1000판 실측과,
거기서 따라 나오는 「8판 중 둘 이상이 무너질 확률」:

| `k` | S5 판당 `w<10` | 8판 중 2판 이상 | 배치 백분위(S1~S5) | 기대 `k/(k+1)` |
|---|---|---|---|---|
| 1 (옛 동작) | 17/1000 (1.7%) | **0.756%** | .51 .50 .48 .48 .51 | 0.50 |
| **2 (채택)** | **6/1000 (0.6%)** | **0.098%** | .62 .67 .67 .67 .67 | **0.67** |
| 3 | 2/1000 (0.2%) | 0.011% | .69 .75 .75 .76 .75 | 0.75 |
| 4 | — | — | .72 .80 .79 .82 .81 | 0.80 |
| argmax(k→∞) | — | — | ~1.0 전부 | 1.00 |

꼬리는 1000판 · 백분위는 시드 12345 · 6시행(`node tools/place.js 1 2 3 4`)이다.
「8판 중 2판 이상」은 앞 칸에서 이항으로 계산한 값이고, 그게 곧 두 게이트가 걸릴
확률이다 — `w[1] >= min(10, 중앙값)` 은 정확히 「2판 이상 무너졌는가」를 묻는다.

**`argmax` 행은 상한 앵커가 아니다.** 「완벽하게 놓으면 기준선이 어디까지 무너지는가」를
재려고 넣었는데 **안 무너진다** — 실측에서 S3 중앙값이 오히려 27 → 25 로 나빠졌다.
매번 커버 최대 칸을 고르면 최고 칸이 차고 그 옆이 다음 최고라 **타워가 한 주머니에
겹겹이 쌓이고 경로의 나머지가 무방비**가 되기 때문이다. 커버는 근시안이라 **이미 덮인
경로 칸을 또 덮는 것을 할인하지 않는다.** 그래서 이 행이 말하는 것은 상한이 아니라
「이 지표를 극단까지 밀면 뭉쳐서 손해」다. k-표본이 이걸 피하는 이유는 **후보를 난수로
뽑아 흩어짐이 남기** 때문이다.

**2 를 고른 근거는 최소성이다.** 게이트가 걸릴 확률을 0.756% → 0.098% 로 내려 이
스위트의 다른 무작위 요인(실측 약 3%) 아래로 충분히 보낸다. 3 으로 올려 더 얻는
0.09%p 의 대가는 그리디가 더 잘 놓는 것이고, 그건 `tune` 실배포 행의 진도를 더 밀어
올린다(스테이지3 사망웨이브 중앙값 27 → 29. 지금 진도 `p` 는 0.93 — `tune` 의 5시행·
랜덤덱 표본이다. #35 이전 `p` 는 그 열이 없던 시절이라 다시 못 잰다). **「대충 하는 플레이어」라는
이 시뮬의 정의가 상한이다.**

**꼬리가 0 이 된 것은 아니다 — 0.6% 가 남는다.** 200판으로 재면 0/200 이 나오지만
그건 30% 확률로 그렇게 보이는 것뿐이라 「없앴다」의 근거가 못 된다. 남은 것을 마저
없애려면 커버 말고 **이미 덮인 곳을 할인하는 자**가 필요하고(위 `argmax` 문단이 그
자가 왜 필요한지를 보여 준다), 그건 난이도 재조정과 같이 볼 일이다.

**배치 백분위는 난이도와 독립인 자다.** 사망 웨이브는 배치 말고도 열 가지가 같이
움직이므로, 배치 규칙이 조용히 죽어도 난이도만 봐서는 못 잡는다(#31 의 「지은 종류
평균」이 같은 자리였다). `node tools/place.js` 가 이 표를 찍고 `npm test` 가
**상·하한 양쪽**으로 잠근다 — 하한은 정책이 빠지는 것을, 상한은 **최적으로 가는 것**을
막는다. 백분위는 동률을 반씩 세는 midrank 라 균등 난수가 분포 모양과 무관하게 정확히
0.5 이고, k-표본 최고는 순서통계량이라 `k/(k+1)` 이 **해석적으로** 나온다 — 그래서
이 표는 「높을수록 좋다」가 아니라 **「실측이 `k/(k+1)` 과 맞는가」**로 읽는다.
① 외곽 도로만 낮은 것은 커버 편차가 0.82 라 동률이 많아서다(의도된 성질).

### 경로 형태 (2스테이지 예시)

```
■ ■ ■ ■ ■ ■ ■     통로 사이 2행 띠가 배치 공간이다.
▲ ■ ■ ■ ◆ ◆ ■     통로 간격을 2행으로 좁히면 남는 띠가 1행뿐이라
· ■ ◆ ■ ◆ ◆ ■     5성(2x2)을 아예 못 놓는다.
· ■ ■ ■ ■ ■ ■     경로 밀도와 2x2 주머니는 정면으로 충돌한다.
  · ▲ ■ ▲ ▲ ·
    · ■ ▲ ▲ ·     ◆ 최상급 자리(경로 7칸+)
    · ■ ■ ■ ■     ▲ 좋음(5-6)   · 보통(3-4)   공백 거의 못 침
      · ▲ ▲ ■
· · · ▲ ▲ ■
■ ■ ■ ■ ■ ■ ■
```

### 크기

- 1~4성: 1칸
- 5성 이상: **2x2**
- 조폐소는 예외로 5성도 1칸 (경제타워가 공간까지 먹으면 아무도 안 씀)

공간 압박이 **5성 진입 시점에만** 발생하도록 설계 — 매 합성마다 테트리스 안 하게.

**행 개방**: w8 / w15 클리어 시 두 행씩. 시작 24칸(2x2 자리 10곳) → 최종 36칸(15곳).
잠긴 구역에 **언제 열리는지 적어 둔다** — 규칙이 있는데 안 보이면 그냥 못 쓰는 칸으로 읽힌다.
개방이 없으면 6성 이상이 물리적으로 불가능하다. 6성 하나를 만들려면 5성 두 개가
동시에 보드에 있어야 하는데 그것만 8칸이다.

### 딜 타입

`물리`(방어력 적용) / `마법`(마방 적용) / `순수`(둘 다 무시)

### 방어력 공식 (워3 방식)

```js
const reduction = armor * 0.06 / (1 + 0.06 * Math.abs(armor));
const dealt = raw * (1 - reduction);
```

방어력 40 → 70.6% 감소, 10 → 37.5% 감소. 방깎으로 40→10 만들면 데미지 **2.13배**.
파쇄(B1)로 -25까지 내리면 추가로 1.6배. 체감 확실하고 무한 인플레 없음.

### 정수

분기 재선택 자원. 웨이브 보상 + 조폐소로 획득. 판당 3~4개.
"5성 박았는데 편성이 바뀜" 스트레스의 안전밸브.

### 경제 / 웨이브

```
시작 골드:   120
적 HP:      100 * 1.16^wave        (w30 ≈ 8,600)
클리어 골드: 70 * 1.11^wave
소환 비용:  10 + 2*(누적 소환수)     웨이브마다 -30% 감쇠
합성 비용:  8 * 합성 전 성급
웨이브 간격: 20초
빨리 보내기: 보상 없음
```

빨리 보내기에 골드를 붙이면 **항상 누르는 게 정답**이라 선택이 아니게 된다.
대기 시간을 건너뛰는 것 자체가 이득이므로 보상은 없다. 대신 그만큼
클리어 골드를 올려서(50 → 70) 총수입을 맞췄다.

### 밸런스는 시뮬레이션으로 역산한다

`node tools/tune.js` — 헤드리스로 게임을 돌리고, **그리디 플레이어**(소환할 수 있으면
소환하고 합칠 수 있으면 합치는, 대충 하는 플레이)가 몇 웨이브에서 죽는지로 난이도를 잰다.
이 플레이어가 클리어해버리면 너무 쉬운 것이고, 초반에 죽으면 잘해도 못 깨는 것이다.

**난이도 기준선은 평균 덱이 아니라 최고 덱이다.** 플레이어는 좋은 덱을 고르기 때문이다.

**어느 스테이지로 재느냐가 이 절의 전부다.** 안 적어 뒀다가 사고가 났다(#28) — 아래
「클리어한 덱 0/35」를 스테이지1 에서 다시 재 보고 「34/35 니까 밸런스가 망가졌다」고
읽었고, 하마터면 멀쩡한 `GOLD_BASE` 를 내려 첫 스테이지를 못 열게 만들 뻔했다.
**스테이지1 이 대충 해도 깨지는 건 의도다.** 스테이지는 깨야 다음이 열리기 때문이다.

35개 덱 조합을 각 6회씩, 네 스테이지 전부. 클리어는 99 로 센다.

**시딩 방식을 반드시 같이 적는다 — 이 표는 그것 없이는 재현이 안 된다.** `tools/tune.js`
의 `run()` 과 같은 관례로 **덱마다 시드 12345 를 다시 박는다**(스윕 전체에 한 번이
아니다). 덱마다 같은 난수열을 주어야 덱끼리 같은 판으로 겨루고(짝지은 비교), 덱을
하나 빼도 나머지가 안 흔들린다. 아래 「계단」 문단이 이 선택에 얼마나 민감한지를 적는다.

```
스테이지  총웨이브  전체평균  최고덱  최저덱  클리어덱  클리어판
   1        20       98.2    99.0    85.8    35/35    208/210   ← 대충 해도 깨진다(의도)
   2        25       65.5    99.0    22.7    29/35    116/210   ← 클리어판 116/210 = 55%
   3        30       29.0    53.0    23.2     4/35      5/210
   4        30       30.1    64.0    24.7     4/35      6/210
   5        30       22.6    28.8    18.2     0/35      0/210
   6        30       21.1    29.0    11.8     0/35      0/210   ← 가장 어렵다
```

**이 표는 2026-08 「종류별 자리 점수」(#48) 뒤의 값이다.** 그 직전(#35 세대)에는
98.6 / 66.1 / 28.3 / 30.5 / 22.6 / 21.2 에 클리어덱 35 / 28 / 2 / 5 / 0 / 0 이었다.
**여섯 판 전부 거의 안 움직였다** — 진도 `p` 는 ⑥ 이 0.71 → 0.70 인 것 하나 빼고
소수 둘째 자리까지 같다. 움직인 것은 소수 첫째 자리의 전체평균과 한 자리 수의
클리어판이라 **덱 단위 노이즈와 같은 자리수**다. #48 은 그리디가 박격포·마력로를
놓는 자를 바꿨는데(§판이 어느 타워를 좋아하는가) **난이도 눈금은 그걸 거의 못 본다.**

**아래 「이 표는 #35 뒤의 값이다」 문단은 그대로 둔다** — 세대 이력이라 지우면
「눈금이 틀렸다」와 「자가 바뀌었다」를 다음 사람이 못 가른다.

**이 표는 2026-08 「배치 정책」(#35) 뒤의 값이고, 5스테이지 행이 처음 붙었다.**
그 전(#31 뒤 · 균등 난수 배치)에는 96.4 / 60.1 / 30.1 / 28.6 에 클리어덱
35 / 23 / 6 / 2 였다. 여기서도 **`index.html` 은 한 줄도 안 고쳤다** — 움직인 것은
`tools/sim.js` 의 자리 고르기이고, 그리디가 균등 난수로 놓던 것을 「후보 두 곳을 보고
커버가 큰 쪽」으로 바꿨다(`SUMMON_SAMPLES = 2`). 그러니 옛 값과의 차이는 밸런스
변경이 아니라 **옛 표가 자리를 안 보는 플레이를 재고 있었다**는 뜻으로 읽어야 한다.
#31 의 「셋째 종류」와 같은 종류의 이동이다(그때는 83.9 / 34.9 / 25.5 / 27.8 ·
클리어덱 34 / 17 / 0 / 0 에서 위 옛 값으로 갔다).

**옛 값을 그대로 재현해서 대조했다.** 같은 스크립트에 `samples = 1`(퇴화값)을 주면
96.4 / 60.1 / 30.1 / 28.6 · 35/23/6/2 가 **한 자리도 안 틀리게** 나온다. 두 표가 같은
자로 잰 것이라는 근거가 이것이고, 「좋아졌다/나빠졌다」를 읽어도 되는 이유다.

#### 판이 어느 타워를 좋아하는가 — `npm run affinity`

난이도와 **직교하는 자**다. 같은 판 안에서 어느 타워가 값을 하는지를 잰다.

```
affinity(판, 타워) = mean(진도 p | 그 타워를 든 15덱) − mean(p | 안 든 20덱)
```

덱 크기가 3 이라 한 종류를 든 덱은 `C(6,2)=15` · 안 든 덱은 `C(6,3)=20` 으로
어느 종류를 봐도 분할이 같다. 그래서 열끼리 나란히 읽어도 된다.

**포화된 판은 아무것도 안 잰다.** ① 외곽 도로는 35덱이 전부 클리어해 `p` 가 모두
1.00 이라 **모든 열이 정확히 +0.000** 이다 — 「타워를 안 가린다」가 아니라 「이 자로는
아무것도 안 재고 있다」다. 도구가 행마다 `포화` 를 박는 이유이고, §난이도의 눈금이
`상한`/`바닥` 을 박는 것과 같은 규칙이다.

35덱 x 8회 · 시드 12345(덱마다 재박음) · `SUMMON_SAMPLES=2` · #48 뒤:

| 판 | 클리어 | 파쇄자 | 침식자 | 서리탑 | **박격포** | 관측소 | **마력로** | 조폐소 |
|---|---|---|---|---|---|---|---|---|
| ① 외곽 도로 | 99.3% **포화** | +.000 | +.000 | +.000 | +.000 | +.000 | +.000 | +.000 |
| ② 이중 병목 | 53.6% | +.009 | −.019 | −.007 | −.002 | −.003 | +.000 | +.023 |
| ③ 갈래길 | 3.2% | +.007 | −.019 | −.010 | **−.060** | −.020 | −.018 | +.119 |
| ④ 역류 | 2.5% | +.020 | +.001 | +.011 | **−.049** | −.008 | −.030 | +.055 |
| ⑤ 분수령 | 0.0% | −.004 | −.009 | +.021 | **−.123** | −.013 | −.052 | +.181 |
| ⑥ 합수 | 0.0% | +.026 | +.013 | +.033 | **−.165** | +.042 | −.073 | +.124 |

**판이 어려워질수록 박격포와 마력로만 음수로 벌어진다.** 그 둘이 정확히 `KINDS` 의
`how` 가 자리를 지정하는 종류다(「적이 줄지어 오는 직선을 덮는다」 · 「통로와 나란히
놓아야 한 줄을 꿴다」). **조폐소가 전 판 최강인 것은 별개 문제다** — 경제 눈덩이라
자리와 무관하고, 이 표에서 가장 큰 수인데 아직 아무 티켓도 안 겨눈다.

##### 자리 고르는 자를 종류별로 바꿔 봤다 — **거의 안 움직였다** (#48)

위 음수의 원인이 「일곱 종이 커버 하나로 자리를 고른다」일 것이라는 가설을 세우고,
박격포는 폭발이 덮는 경로 · 마력로는 빔이 꿰는 경로로 자를 갈랐다
(`tools/sim.js` §종류별 자리 점수). **자는 실제로 크게 달라졌다:**

| | ① | ② | ③ | ④ | ⑤ | ⑥ |
|---|---|---|---|---|---|---|
| 폭발 자의 커버 대비 순위상관 ρ | 0.10 | 0.72 | 0.56 | 0.73 | 0.62 | 0.57 |
| 관통 자의 커버 대비 순위상관 ρ | −0.41 | 0.58 | 0.21 | 0.56 | 0.56 | 0.64 |
| 두 칸 중 판정이 갈리는 비율(폭발/관통) | 27/35% | 20/22% | 22/32% | 19/22% | 20/23% | 24/20% |

박격포가 고른 자리의 **폭발 백분위**도 올라왔다(커버로 고를 때 → 폭발로 고를 때):
① .53→.65 · ② .59→.67 · ③ .59→.69 · ④ .63→.69 · ⑤ .59→.66 · ⑥ .60→.66.
즉 **다섯 소환 중 한 번꼴로 다른 칸에 놓이고, 자기 자로 재면 확실히 더 좋은 칸이다.**

**그런데 affinity 는 거의 안 움직였다.** 앞의 값이 #35 세대, 뒤가 #48:

| 판 | 박격포 | 마력로 |
|---|---|---|
| ② 이중 병목 | −.008 → −.002 | −.001 → +.000 |
| ③ 갈래길 | −.060 → −.060 | −.022 → −.018 |
| ④ 역류 | −.053 → −.049 | −.036 → −.030 |
| ⑤ 분수령 | −.111 → **−.123** | −.058 → −.052 |
| ⑥ 합수 | −.175 → −.165 | −.069 → **−.073** |

**열 칸 중 둘이 오히려 나빠졌고**(⑤ 박격포 · ⑥ 마력로), 나머지의 개선폭도
0.004~0.010 이라 이 표가 가르는 신호(⑥ 박격포 −.165)의 **20분의 1** 이다.
난이도 곡선도 진도 `p` 가 여섯 판 전부 ±0.01 안이다.

**그래서 「배치가 원인」은 기각한다.** 자를 실제로 20% 갈아 끼우고 백분위를
0.06~0.13 올렸는데 affinity 가 안 따라오면, 박격포·마력로의 음수를 만드는 것은
자리가 아니다. 남은 후보는 셋이고 **아직 아무것도 안 쟀다**:

- **`SUMMON_SAMPLES = 2` 가 상한이다.** 그리디는 무작위 후보 **두 곳** 중에서만
  고르므로 자를 아무리 좋게 만들어도 배치 품질의 상한이 낮다. 자가 아니라 `k` 가
  병목일 수 있는데, `k` 를 올리는 것은 §꼬리를 재는 자가 「그 이상 잘하게 만들지
  않는다」로 이미 막아 둔 축이다 — **자를 고치는 것으로는 이 벽을 못 넘는다**
- **분기·성급이 자리보다 크다.** 박격포의 5성 탄막(A1)은 계수 문제로 한 번 갈아엎은
  적이 있고(§경제/웨이브 위 `BARRAGE_SHARE`), 마력로는 `cd 2.5` 로 발사가 드물다
- **그리디가 오라·조폐 쪽으로 기운다.** 조폐소가 전 판 +.05~+.18 로 압도적이라
  공격 타워끼리의 차이가 그 그늘에 있다

**이 절을 「실패」로 지우지 마라.** 자를 안 바꾼 다섯 종류가 안 움직인 것과 짝지어
읽으면, 이 표는 **「박격포 약세는 배치 문제가 아니다」를 실측으로 못박은 것**이다.
다음 사람이 같은 가설로 또 자를 만지지 않게 하는 것이 이 절의 값이다.

#### 난이도의 눈금 — 판마다 읽을 지표가 다르다

**눈금의 정본은 이 절과 `npm run curve` 둘뿐이다.** 한때 셋으로 갈라져 있었다 —
`tools/tune.js` 의 절대 웨이브 밴드, `tools/test.js` 밸런스 블록의 8회 게이트,
`tools/stagetune.js` 머리의 목표 줄. 셋이 서로 다른 것을 재면서 서로를 몰랐고
판·덱·표본·시딩이 전부 달랐다(#37). 지금 역할은 이렇게 갈라 놓았다.

| 도구 | 재는 것 | 눈금인가 |
|---|---|---|
| `npm run curve` | 35덱 x 6회 = 210판 · 여섯 판 전부 · 시드 12345(덱마다 재박음) | **그렇다** |
| `npm run tune` | 축 하나를 움직였을 때의 진도 이동량 `Δp`(민감도) | 아니다 — 목표값이 없다 |
| `npm test` 밸런스 | 고정덱 1개 · 8회 · 무시드 | 아니다 — **게이트다** |

**어느 지표도 여섯 판 전부에서 살아 있지 않다.** 그래서 판마다 읽을 칸이 다르다.

| 판 | 총웨이브 | 클리어판 비율 | 진도 `p` (210판·35덱) | 읽는 칸 |
|---|---|---|---|---|
| ① 외곽 도로 | 20 | 208/210 = **99.0%** | 1.00 (상한 포화) | 클리어판 비율 |
| ② 이중 병목 | 25 | 116/210 = **55%** | 0.98 (상한 포화) | 클리어판 비율 |
| ③ 갈래길 | 30 | 5/210 = 2.4% (바닥) | **0.91** | 진도 `p` |
| ④ 역류 | 30 | 6/210 = 2.9% (바닥) | **0.94** | 진도 `p` |
| ⑤ 분수령 | 30 | 0/210 = 0.0% (바닥) | **0.75** | 진도 `p` |
| ⑥ 합수 | 30 | 0/210 = 0.0% (바닥) | **0.70** | 진도 `p` |

`p = 평균웨이브 / 총웨이브` 이고 **클리어는 총웨이브로 센다 — 99 센티넬을 안 쓴다.**
99 로 세면 ① 의 `p` 가 4.9 로 튀어 「진도」라는 말이 뜻을 잃는다. 위 표의 `전체평균`
열은 여전히 99 센티넬인데, 그건 세대 간 대조용 호환 열이고 **눈금이 아니다.**
웨이브 수를 그냥 견주면 안 되는 이유도 여기 있다 — 총웨이브가 판마다 20 · 25 · 30 이라
같은 「25웨이브」가 ② 에서는 클리어이고 ⑤ 에서는 진도 0.83 이다.

**포화된 칸을 눈금으로 삼으면 그 뒤의 판단이 전부 헛돈다.** 이 리포는 그 함정에 이미
두 번 빠졌다 — `parity.js` 가 스테이지1 에서 재서 기여도가 0 으로 수렴한 것(아래
§타워 대등성 ②), 그리고 ⑤ 의 `hpMult` 를 1.0/0.8/0.7/0.6 으로 훑었는데
「클리어 0/8 · 중앙 w28~30」으로 전부 같아 못 갈린 것(`index.html` ⑤ 분수령 주석)이다.
**둘 다 「차이가 없다」가 아니라 「그 자로는 아무것도 안 재고 있다」였다.**

##### 절대 숫자는 그리디 능력 세대에 묶인다

`index.html` 을 한 줄도 안 고쳐도 `tools/sim.js` 가 바뀌면 위 표가 통째로 움직인다.
#31 은 그리디에게 덱의 셋째 종류를 짓게 했고, #35 는 자리를 보고 놓게 했다. 그래서
**값을 인용할 때는 세대를 같이 적어야 한다** — `curve` 가 행마다
`35덱x6 · seed12345(덱마다 재박음) · SUMMON_SAMPLES=2` 를 박는 이유다. 그게 없으면
다음 사람이 「눈금이 틀렸다」와 「자가 바뀌었다」를 못 가른다.

| 세대 | S3 전체평균 | S4 전체평균 | 판정 |
|---|---|---|---|
| #31 이전 | 25.5 | 27.8 | 3 이 어렵다 |
| #31 후 | 30.1 | **28.6** | **4 가 어렵다** |
| #35 후 | 28.3 | 30.5 | 3 이 어렵다 |
| #48 후 (지금) | 29.0 | 30.1 | 3 이 어렵다 |

**S3 과 S4 의 순서는 세대마다 뒤집힌다.** 아래 「계단의 3 대 4」가 「시딩만 바꿔도
뒤집힌다 → 판정 불가」라고 적어 둔 것이 이제 **시딩을 안 바꿔도 뒤집힌다**로 더
나빠졌다. 표본을 바꿔도 뒤집힌다 — `npm test` 의 8판 고정덱은 S4 를 더 어렵다고
하고(`p` 중앙 S3 0.996 대 S4 0.988) 위 210판 표는 반대라고 한다(0.91 대 0.94).
그래서 `tools/test.js` 는 인접 쌍을 하드 게이트가 아니라 `known()` 으로 둔다.

**세대가 바뀌어도 안 흔들린 것은 하나뿐이다: ① 이 가장 쉽다.** 세 세대 전부에서
그렇고, 그건 의도다(스테이지는 깨야 다음이 열린다 — #28).

**「마지막 판이 가장 어렵다」는 불변이 아니다.** 판이 넷이던 시절 마지막 판은 S4 였고,
#31 이전 세대에서 S4(27.8)는 S3(25.5)보다 **쉬웠다** — 그때 이 게이트는 아예 성립하지
않았다. 다음 세대에서 우연히 뒤집혀 성립했을 뿐이다. 지금은 ⑤ 가 22.6 으로
다음 판(28.3)과 5.7웨이브 벌어져 있어 노이즈(1~2웨이브)보다 한참 크고, 그래서
`npm test` 가 **두 끝**(첫 판이 가장 쉽다 · 마지막 판이 가장 어렵다)을 `p` 로 잠근다.
**판이 또 붙으면 이 게이트가 겨누는 대상이 통째로 옮겨 가므로 그때 다시 재야 한다** —
#33 에서 ⑤ 를 붙였을 때 S4 래칫이 정확히 그렇게 새 판으로 옮겨 가 S4 를 놓쳤다.

그 사이(②③④ 의 상대 순서)는 **지금 근거가 없다.** 시행수를 올려 신뢰구간을 붙이기
전에는 「계단이 단조가 아니다」를 고칠 근거도, 그대로 두어도 된다는 근거도 못 만든다.

**계단이 3 과 4 에서 서로 자리를 바꿨다.** 3 이 30.1 → 28.3(클리어덱 6 → 2)으로
어려워지고 4 가 28.6 → 30.5(2 → 5)로 쉬워졌다. 아래 「계단의 3 대 4」가 **시딩만
바꿔도 순서가 뒤집힌다**고 적어 둔 그 차이(1~2웨이브)와 같은 자리수라, 이 표 하나로
「이제 3 이 더 어렵다」고 읽으면 안 된다. 여전히 판정 불가다.

`node tools/tune.js` 의 실배포 행도 같이 움직였다: 스테이지3 사망웨이브 중앙값이
27 에서 **29** 로 올라갔다(시드 12345 · 5시행 · 지금 진도 `p` 0.93). #35 이전의 `p` 는
그 열이 없던 시절이라 다시 못 잰다 — 세대가 다르면 값이 아니라 **자**가 다르다.
즉 지금 이 게임은 그리디에게 너무 쉽고, #35 가 그리디를 조금 낫게 만들면서 더 쉬워졌다.
**예측대로다 — 그리고 그게 이 티켓에서 `tune.js` 를 한 줄도 안 건드린 이유다.**
**난이도 재조정은 별도 티켓이다** — 시뮬의 결함을 고친 것과 같은 커밋에서 `GOLD_BASE`
나 `DMG_SCALE` 을 만지면 무엇이 무엇을 움직였는지 다시 못 가른다(#28 이 그렇게 났다).

**난이도의 기준은 여전히 스테이지3 이다.** `tools/tune.js` 가 재는 판은 스테이지3 으로
박혀 있고 이 티켓에서 안 옮겼다. **「그럼 이제 3 과 4 중 어느 쪽이 더 어려운가」는
이 표로 답할 수 없다** — 아래 「계단」 문단이 그 이유다.

같은 형식으로 스테이지3 만 떼면 이렇다:

```
최고 덱  파쇄+관측+조폐  평균 53.0
전체 평균                  28.3
최저 덱  침식+박격+관측  평균 23.5
클리어한 덱  2/35   (클리어판 3/210)   ← 스테이지3 기준
```

**최고덱이 53.0 인 것은 「53웨이브까지 버틴다」가 아니다.** 클리어를 99 로 세는
관례라 6판 중 몇 판이 클리어(30웨이브 완주)하면 평균이 통째로 튄다. 최고덱은 여전히
조폐소(경제)가 든 덱이 가져간다 — #31 로 셋째 종류를 짓게 되면서 조폐소가 처음으로
실제로 판에 섰고, #35 뒤에도 그대로다.

**「0/35」는 낡은 값이 아니었다 — 스테이지가 안 적혀 있었을 뿐이다.** 오래 적혀 있던
23.4 / 20.3 / 18.1 / 0/35 와 그 뒤 값은 4~5웨이브 차이였는데, 그게 그때 올린 타워 강화
(관측소 표적 유지 · 파쇄자 물리 취약 · 박격포 탄막)의 크기였다. 판정(0/35)이 처음으로
바뀐 것이 위 #31 이고, 그건 밸런스가 아니라 **시뮬이 재던 대상**이 바뀐 것이다.

**계단의 3 대 4 는 지금 못 가른다 — 시딩 방식에 따라 순서가 뒤집힌다.** 위 표(덱마다
시드 재박음)는 4 가 더 어렵다고 하는데, 같은 코드·같은 시드에서 시딩만 바꾸면 반대가
나온다:

| 시딩 | S3 전체평균 | S4 전체평균 | 결론 |
|---|---|---|---|
| **덱마다 12345 재박음** (위 표의 방식) | 30.1 | **28.6** | 4 가 더 어렵다 |
| 스윕 전체에 12345 1회 | **28.8** | 30.7 | 3 이 더 어렵다 |
| 무시드 | **28.4** | 30.9 | 3 이 더 어렵다 |

세 표본 중 둘이 「3 이 더 어렵다」이므로 **오래 적혀 있던 「스테이지4 는 3 보다 쉽다」를
뒤집을 근거로 쓰기에는 모자라다.** 차이(1~2웨이브)가 같은 덱 반복 노이즈와 같은 자리수라
셋 다 노이즈 안일 가능성이 크다. 이걸 진짜로 가르려면 시행수를 올려 신뢰구간을 붙여야
하고, 그건 난이도 재조정 티켓이 할 일이다. **기준판(`STAGE = 2`)을 옮기는 근거로 이
표를 쓰지 마라.** hpMult 가 0.6(S3) 대 0.8(S4) 인데도 계단이 단조가 아닌 것 자체는
그대로 남은 문제다. `npm test` 는 이 차이를 **일부러 안 잡는다** — 두 끝만 게이트로
잠그고 인접 쌍은 `known()` 으로 값만 찍는다(#37). 노이즈 크기의 차이를 하드 게이트로
만들면 무고한 빨간불이 되기 때문이고, 가르려면 시행수부터 올려야 한다.

덱 간 편차 2.01 vs 같은 덱 반복 노이즈 0.79(스테이지3, 클리어를 30 으로 자름) —
**신호 대 노이즈가 1.09배에서 2.5배로 벌어졌다.** 셋째 종류가 안 지어지던 시절에는
편차 1.64 대 노이즈 1.51 로 마진이 거의 없었는데, 그건 덱 정체성이 약해서가 아니라
**세 종류 중 하나가 판에 없어서** 덱이 실제로는 두 종류밖에 안 갈렸기 때문이다.
최고−최저는 6.2 → 5.7 로 오히려 좁아 보이는데, 이건 상한(30)에 눌린 값이다 —
클리어하는 덱이 6개라 위쪽이 잘린다. 덱 차이를 더 벌리고 싶으면 여기가 손볼
자리이되, 먼저 난이도를 조여 상한에서 떼어 놔야 값이 읽힌다.
(스테이지1 에서는 아무것도 안 보인다. 거의 모든 덱이 클리어에 몰려 상한에 눌리기
때문이다 — 덱 차이를 재려면 반드시 뒤 스테이지에서 재야 한다. 이 함정에 지금
`tools/parity.js` 가 걸려 있다. 아래 「타워 대등성」을 볼 것.)

### 셋째 종류 — 그리디는 덱의 셋째 종류를 안 짓고 있었다 (#31, 해결)

이 표를 뜨면서 나온 것이다. `파쇄+침식+X` 다섯 덱이 X 와 무관하게 **한 웨이브까지
똑같이** 끝났다. 원인은 `sim.js` `pickKind` 의 한 줄이었다 — 「짝이 안 맞는 최저
성급」이 없는 종류를 `Infinity` 로 떨어뜨렸는데, **한 대도 없는 종류가 정확히 그
경우**라 덱의 셋째 원소가 영영 꼴찌였다. 「덱 3종」으로 재는 모든 수치가 사실상
2종을 재고 있었다.

고친 것은 `own.length === 0` 을 최우선으로 갈라낸 것 하나다. 타워는 있는데 짝이 다
맞는 종류(2x2 자리가 없어 5성 둘이 남은 경우 등)는 그대로 후순위다 — 그건 더 지어도
이을 데가 없는 종류라 맞다.

실측(스테이지3 · 35덱 x 6회 · 시드 12345):

| | 전 | 후 |
|---|---|---|
| 지은 종류 평균 | 2.00 / 3 | **3.00 / 3** |
| 3종을 6회 다 지은 덱 | 0 / 35 | **35 / 35** |
| 앞 두 종류가 같은 묶음이 갈리는가 | 10묶음 전부 max−min = 0.00 | **10묶음 전부 ≥ 0.5** (최소 2.67) |

**웨이브가 올라간 것은 판정 근거가 아니다.** 종류를 더 짓게 하면 난수 스트림이
통째로 밀리므로 「좋아졌다」는 말은 그 자체로는 아무 뜻이 없다(#28). 그래서 시드
10개로 old/new 를 **짝지어** 재서 스테이지3 전체평균 차이의 부호가 10/10 일치하는
것(+2.47 ~ +4.61)을 먼저 확인했다. 부호가 갈렸다면 그건 개선이 아니라 밀림이다.

### 두 번째 지표: 타워 대등성

`node tools/parity.js` — **그 타워가 든 덱들의 평균 사망 웨이브 − 안 든 덱들의 평균**.
0 에 가까울수록 대등하고, 크면 그 타워가 정답이거나 함정이라는 뜻이다.
한 타워가 정답이면 덱도 합성도 고민할 필요가 없어진다.

덱 3종으로 재는 이유는 감도다. 5종이면 어느 두 덱이든 최소 3종이 겹쳐서
타워별 차이가 노이즈에 묻힌다.

실측 (시드 12345 · 7시행, `npm test` 의 대등성 게이트와 같은 조건):

| 분기 | 기여도 폭 | 전(#35 이전) | 전(#31 이전) | 주도하는 타워 |
|---|---|---|---|---|
| A/A1 | 1.17 | 1.33 | 3.20 | — |
| A/A2 | 1.52 | 1.50 | 2.20 | — |
| B/B1 | 0.55 | 0.72 | **7.88** | (#31 이전: 서리 −4.71) |
| B/B2 | **0.00** | 0.00 | 5.05 | — |

**네 분기 전부 임계 6 아래이고 KNOWN 은 비었다.** 오래 B/B1 이 7.88 로 `KNOWN` 에
박혀 있었고(그전에는 16.51, 침식자 혼자 −3.5 ~ −9.6), 「폭이 6 아래로 내려가면
하드 게이트로 승격할 것」이라고 적어 뒀다. #31 뒤 재실측값이 0.72 라 승격했다.

**#35(배치 정책)로는 이 표가 거의 안 움직였다** — 최악 폭 1.50 → 1.52 다. 배치를
고치면 판이 통째로 조금 쉬워지는데 그건 덱 전부에 같이 걸리므로 「든 덱 − 안 든 덱」
차이에는 안 남는다. 짝지은 차이 지표라 공통 이동이 상쇄되는 것이고, 그 성질이 여기서
확인된 셈이다.

**그런데 서리탑이 균형을 찾은 게 아니다.** 두 가지가 동시에 일어났고 둘 다 적어 둔다.

① **옛 값은 없는 타워를 재고 있었다.** `parity.js` 의 `combos(K,3)` 은 `K` 순서를
보존하므로 조폐소는 자기가 든 15덱 **전부에서 `deck[2]`** 였고, 셋째 종류를 안 짓던
그리디는 그 15판에서 조폐소를 한 대도 안 세웠다. 「그 타워가 든 덱」의 상당수가
그 타워가 **존재하지 않는 판**이었다. 7.88 은 그 위에 얹힌 값이다.

② **지금 값이 작은 것은 감도가 죽어서이기도 하다.** `measure()` 는 `greedy` 에
`opts.stage` 를 안 넘겨 **스테이지1** 에서 잰다(기본값 0). 3종을 짓게 된 그리디는
거기서 거의 다 클리어한다 — 실측 B/B2 245/245판 클리어, B/B1 241/245. 전부 상한(30)
에 눌리면 기여도 차가 0 으로 수렴하므로 **B/B2 의 0.00 은 「완벽히 대등」이 아니라
「아무것도 안 재고 있다」**는 뜻이다. 바로 위 문단이 「덱 차이를 재려면 반드시 뒤
스테이지에서 재야 한다」고 적어 둔 그 함정에 이 도구가 걸려 있다.

그래서 이 게이트가 지금 잡아 주는 것은 「폭이 6 을 넘게 벌어지는 회귀」뿐이고 미세한
불균형은 못 본다. **`parity.js` 를 어느 스테이지에서 재느냐가 다음에 볼 자리다** —
난이도를 다시 조여 상한에서 떼어 놓는 작업과 같이 봐야 한다. 서리탑이 B1(연쇄 빙결)
에서만 −4.71 이던 이유는 그 뒤에도 답이 안 나온 채로 남아 있다(지금은 −0.4 로 보이지만
위 ①② 때문에 그 값을 「해결됐다」로 읽으면 안 된다).

### 이 시뮬이 잡아낸 것들

숫자를 만지면 반드시 다시 돌릴 것. 손으로 맞춘 값은 네 번 틀렸다.

1. 화력이 필요치의 1/100이었다 — 적 HP는 지수인데 화력은 소환 횟수에 비례해 선형이었다
2. 7종 랜덤이면 5성이 영원히 안 나온다 → 덱 도입
3. 보드가 안 늘어나면 6성이 물리적으로 불가능하다 → 행 개방
4. 조폐소 기본 수입 코드가 아예 없었고, 침식자는 절반의 적에게 무효였다 → 서포트 3종 전부 마이너스 기여

---

## 2.5 그림

스프라이트는 **32x32 문자열 도트**로 적고 오프스크린 캔버스에 한 번 구워서 캐시한다(`SPR`).
확대는 `imageSmoothingEnabled=false`. 이미지 파일은 안 써도 되지만 금지는 아니다 —
직접 그릴 일이 생기면 PNG 시트로 갈아끼우면 된다.

8x8 → 16x16 → 32x32 로 두 번 올렸다. 8x8 은 실루엣이 뭉뚝해서 마력로가 사다리로,
중갑이 보병과 같은 덩어리로 읽혔다. 16x16 도 보드 위 표시가 32px 남짓이라 폰(DPR 3)에서
디테일이 남았다. 해상도를 올릴 때는 표시 비율도 같이 올려야 한다 — 지금은 셀의 82%다.

**도트는 손으로 찍지 않고 `tools/sprites.js` 가 만든다.** 32칸짜리 문자열 480줄을
손으로 적으면 폭 오류가 계속 난다. 도형으로 그리고 외곽선을 자동으로 입혀 뽑으므로,
고칠 때는 도형만 만지면 되고 폭은 생성기가 보장한다.

색은 타워·적마다 다르므로 도트에는 **명암만** 적는다 — `0` 외곽선, `1` 어둡게, `2` 기본,
`3` 밝게, `4` 하이라이트. 외곽선이 있어야 어떤 배경 위에서도 실루엣이 선다. 실제 색은 구울 때 주입하므로 도트 한 벌로 전부 그린다.
슬로우·빙결 같은 상태도 색만 바꿔 다시 구우면 된다.

보드도 도트로 그린다. 칸 하나를 16 도트로 보고 바닥·경로를 찍는다. 무늬는 좌표에서
값을 뽑아 만들므로 같은 칸은 늘 같은 모양이 나온다.

**경로는 모서리 타일을 따로 만들지 않는다.** 이웃이 경로가 아닌 면에만 테두리를 넣으면
직선이든 모서리든 갈래길이든 교차로든 이 규칙 하나로 맞는다. 스테이지마다 맵이 다르고
레인이 겹치기도 해서, 변형 타일을 세면 금방 관리가 안 된다.

**성급은 스프라이트로 만들지 않는다.** 7종 x 7성급 = 49장은 관리가 안 된다.
성급은 테두리 색과 크기(5성부터 2x2)로만 표현한다.

실루엣이 서로 겹치지 않는지 테스트로 잠가 둔다 — 색만으로 구분하면
작은 화면이나 색약에서 못 읽는다.

---

## 2.6 처치 파편

적이 사라지는 순간이 화면에서 안 읽혔다. HP 바가 없어지는 게 전부라 **죽인 건지
관문으로 샌 건지가 구분이 안 된다.** 그래서 처치에만 파편을 뿌린다.
**관문 도달(누수)에는 절대 붙이지 않는다** — 적이 새는 걸 처치처럼 보이게 하는 게
이 연출이 낼 수 있는 최악의 거짓말이다. 테스트로 잠가 뒀다.

이 금지는 **보드 층**(파편·셀 좌표) 이야기다. 2.8 의 누수 경고는 화면 층이고
결이 반대(붉은 비네트)라 같은 규칙이 아니다.

| 규약 | 값 | 이유 |
|---|---|---|
| 좌표계 | **셀 좌표** (beams/blasts 와 동일) | 픽셀로 저장하면 화면 크기가 바뀌는 순간 떠 있던 파편만 엉뚱한 자리에 남는다 |
| 수명 | 0.3초 | 처치 프레임은 전체의 1~2%다. 더 길면 잔상이 판을 덮는다 |
| 상한 | **360** | 장판(서리·소이·동상)이 한 프레임에 수십 마리를 죽인다. 배속 4면 프레임당 4번 겹친다 |
| 개수 | `6 + round(r*10)` | 군집 8 · 보병 9 · 정예 10. 몸집이 곧 파편량 |
| 저장 | 링 버퍼 + `alive` 플래그 | `filter` 로 매 프레임 새 배열을 만들면 버스트마다 쓰레기가 쏟아진다 |

**상한을 넘으면 생성을 막지 말고 덮어쓴다.** 막으면 하필 방금 죽은 적의 연출만
통째로 빠진다 — 제일 봐야 할 것이 안 보인다.

### 도형은 딜 타입으로 갈린다

우선순위는 **빙결 > 물리 > 마법 > 그 외**다.

| 조건 | 도형 |
|---|---|
| `frozen > 0` | 각진 삼각 얼음 조각, `#79c0ff`, 중력 1.8배 |
| `physical` | 회전하는 각진 파편, 적 색 |
| `magic` | 확장하는 링 1개 + 원형 잔광 |
| `pure` · 타입 미전달 | 물리와 동일 (fallback) |

`pure`(마력로 B2 순수 광선, 서리탑 A2 동상)와 미전달 fallback 이 없으면
**면역몹 상대 빌드에서만 연출이 사라진다.** 즉사 경로(관측소 B1 처형, 7성 처형
프로토콜)는 `damage()` 를 안 거치므로 딜 타입을 호출부에서 직접 넘겨야 한다.

이건 **속성 상성이 아니다**(7장에서 기각). 타입은 물리/마법/순수 3종뿐이고
"화염 처치는 붉게" 같은 확장은 안 한다.

### 파편은 외곽선을 못 쓴다

2.5 는 "외곽선이 있어야 어떤 배경 위에서도 실루엣이 선다"를 못박았는데,
파편은 셀의 5~10% 크기라 외곽선을 넣을 자리가 없다. 대신 **적 색을 흰색 쪽으로
35% 밀어** 최소 대비를 만든다. 보드 바닥이 `#1a212e`/`#1d2533` 이라 어두운 색을
그대로 뿌리면 묻힌다.

### 연출은 전역 난수를 쓰지 않는다

**`Math.random()` 금지.** 연출 전용 LCG 를 따로 둔다.

관측소 치명(25%), 조폐소 약탈(25%), 소환 자리, 덱·특성 롤이 전부 같은 전역
스트림을 쓴다. 처치할 때마다 난수를 뽑으면 스트림이 밀려서 **밸런스를 한 줄도
안 고쳤는데 시뮬 결과가 바뀐다.** `tools/seedcheck.js` 가 결과 문자열뿐 아니라
**난수 호출 횟수까지** 비교해서 이걸 감시한다(`npm test` 에 포함).

### 그리는 순서

`drawEnemies()` **뒤**, 보드 클립 **안**이다. `drawEffects()` 안에 넣으면 살아있는
적 밑에 깔려서 안 읽힌다 — 죽는 순간의 그림이라 적 위에 있어야 한다.

헤드리스 테스트는 "`render()` 가 안 터지는지"만 보므로 **호출을 통째로 지워도
안 걸린다.** 그래서 `tools/sim.js` 의 캔버스 스텁이 그리기 호출을 기록하고,
파편 수만큼 도형이 나왔는지를 테스트가 센다.

---

## 2.7 타격 3박자

2.6 이 "적이 죽었다"를 읽히게 만들었더니, 그 앞 단계인 **"타워가 지금 일하고 있다"**
가 안 읽히는 게 남았다. 빔 한 줄이 0.09초 떠 있는 게 전부라서, 화면을 봐도
어느 타워가 쏘는 중인지 알 수 없었다. 그래서 한 발을 세 박자로 나눈다.

| 박자 | 무엇 | 수명 | 어디서 |
|---|---|---|---|
| ① 총구 화염 | 발사한 타워의 몸 바깥 끝에 발광점 | **0.07초** | `muzzle()` → `drawEffects()` |
| ② 빔 | 같은 두 점에 글로우 + 흰 코어 **2겹** | 0.09초 (마력로 0.16, 기존값) | `beam()` → `drawEffects()` |
| ③ 착탄 스파크 | 착탄면에서 되튀는 짧은 선 4개 | **0.20초** | `spawnSparks()` → 파티클 풀 |

세 수명이 다른 건 의도다. 화염이 가장 짧아 "출발"로 읽히고, 스파크가 가장 길어
빔이 사라진 뒤에도 "여기 맞았다"가 남는다.

### 수명으로는 배속을 못 막는다 — 래치로 막는다

배속은 `dt` 를 키우지 않고 `update()` 를 여러 번 돈다. 렌더 1프레임 사이에 흐르는
게임시간은 **`min(실제 프레임 간격, 0.05) x 배속`** 이고, x4 최악값은 **0.20초**다.
60fps 를 전제로 `4x1/60 = 0.0667` 로 계산하면 틀린다 — 30fps 기기 x4 는 0.133초이고,
실측에서 화염이 `flash -0.0300 (알파 0)` 으로 **통째로 안 그려졌다.**

**그래서 이 문제는 수명으로 풀지 않는다.** 어떤 값을 골라도 더 낮은 프레임레이트가
그 값을 넘긴다. `muzzle()` 이 `mzShown = false` 를 세우고 그리는 쪽이
`flash > 0 || mzShown === false` 로 게이트한 뒤 `mzShown = true` 로 소모한다 —
"태어났으면 최소 한 프레임은 그려진다"가 프레임레이트와 무관하게 보장된다.
래치로 들어온 프레임은 수명이 이미 다 탔으므로 알파를 최대로 낸다.

`MUZZLE_LIFE 0.07` 은 이제 하한이 아니라 **x1 에서 몇 프레임 보일지**만 정한다
(60fps 에서 약 4프레임). `mzShown` 은 스냅샷에 안 들어간다.

스파크는 래치가 필요 없다. 파티클은 태어난 스텝에서 감쇠하지 않아 한 렌더 프레임에
최대 3번(3x0.05 = 0.15초) 깎이므로, `SPARK_LIFE 0.20` 이면 최악 배속에서도
0.05 가 남아 반드시 한 번은 그려진다.

### 두 겹이라야 빛으로 읽힌다

한 겹은 굵기를 어떻게 잡아도 "선"이다. 넓고 흐린 글로우(알파 x0.45) 위에
얇은 흰 코어를 같은 두 점에 겹쳐 긋는다. `lineCap` 은 건드리지 않는다 —
`ctx` 전역이라 복구를 한 번 빠뜨리면 그 뒤의 모든 `stroke` 로 새어 나간다.

### 스파크 수치

| 값 | 수치 | 이유 |
|---|---|---|
| 발당 개수 | 4 | 처치 파편 8~10(2.6)보다 적어야 처치와 안 헷갈린다 |
| 수명 | 0.20초 | 처치 0.3 보다 짧아야 여파로 읽힌다. 최악 배속 프레임에서도 0.05 남는다 |
| 속도 | 2.2 칸/초 (x0.6~1.4) | 수명 동안 0.44칸. 처치 파편 0.75칸보다 덜 퍼져 착탄점에 붙어 있다 |
| 중력 | 0.4 | 여파는 튀는 그림이지 떨어지는 그림이 아니다 |
| 위치 | 적 중심에서 **타워 쪽으로** `ENEMY[e.kind].r` 민 점 | 빔이 실제로 닿는 표면. 중심에서 뿌리면 몸 안에서 나온 것처럼 보인다 |
| 방향 | 빔 진행의 **역방향 ±60도 부채꼴** | 등방으로 뿌리면 처치 파편과 구분이 안 된다 |
| 색 | `mixHex(타워색, '#ffffff', 0.45)` | 2.6 과 같은 이유로 흰색 쪽으로 민다 |

**색은 적 색이 아니라 타워 색이다.** 처치 파편이 적 색이므로, 같은 지점에 둘이
겹쳐도(명중과 동시에 처치) 누가 여파고 누가 죽음인지 색으로 갈린다.
딜 타입으로 색을 가르지 않는다 — 속성 상성은 7장에서 기각했다.

### 어디에 안 넣는가

이 절의 핵심은 넣는 곳이 아니라 **빼는 곳**이다.

| 경로 | 화염 | 스파크 | 이유 |
|---|---|---|---|
| **서리탑 전부** | **X** | X | 딜이 장판이라 **애초에 발사를 안 한다.** 쿨다운 통과 지점에서 화염을 세우면 아무것도 안 쏘고 화염만 내는 타워가 된다 |
| 조폐소 | X | X | 공격을 안 한다 |
| **마력로 (관통·A1 연쇄)** | O | **X** | 한 발이 선 위 5마리를 동시에 꿴다. 여파를 얹으면 라인 전체가 불꽃 띠가 되어 **관통이 몇 마리를 꿰었는지가 오히려 안 보인다.** 화염 하나로 "여기서 나갔다"는 충분하다 |
| 오라·디버프 (파쇄자 스택, 침식자 감소) | X | X | 발사가 아니다 |
| 장판·지속딜 (서리·소이·동상·소각) | X | X | 매 프레임 들어가므로 연출을 붙이면 화면이 계속 탄다. 2.6 의 "지속딜은 피격 플래시를 안 낸다"와 같은 이유 |
| 박격포 착탄 | X | X | `blasts` 링이 이미 착탄을 맡고 있다 |
| **죽은 적 (정상 킬 · 즉사 2경로)** | O | **X** | 그 자리에 처치 파편 8~10개가 통째로 깔린다. 여파 4개를 얹으면 "맞았다"와 "죽었다"가 한 덩어리가 된다 |
| **관문 도달(누수)** | X | X | 2.6 과 같다. 새는 걸 처치처럼 보이게 하는 게 최악의 거짓말이다 |

**빼는 자리를 구조로 잠갔다.** 스파크는 `beam()` 안에서만 난다. 마력로는 `beam()` 을
안 쓰고 관통 라인을 직접 push 하므로, 여기 두는 것만으로 금지 대상이 구조적으로 빠진다.
`damage()` 에 `!tick` 으로 걸면 마력로 본체와 A1 연쇄가 그대로 통과한다.

**"죽은 적에는 여파 없음"도 호출부가 아니라 `beam()` 의 `!e.dead` 하나로 잠갔다.**
처음에는 즉사 2경로에만 `spark=false` 플래그를 넘겼는데, `damage()` 가 인라인으로
`killEnemy()` 를 부르므로 **훨씬 흔한 정상 킬에서 규칙이 조용히 깨졌다** —
죽이는 한 방은 파편 9개를 뿌린 직후 같은 자리에 스파크 4개를 얹고 있었다.
규칙이 `kill` 이 문법적으로 보이는 두 줄에서만 지켜졌던 것이다. 그래서 즉사 분기는
`killEnemy()` 를 먼저 부르고 `beam()` 을 뒤에 부른다 — 두 경로가 한 규칙을 쓴다.

### 처형 빔을 같이 고쳤다

관측소 B1 처형과 7성 처형 프로토콜은 `beam()` 앞에서 `continue` 해서 **원래도 빔이
안 그려졌다.** 화염이 붙자 "화염은 났는데 선이 없고 적만 터지는" 프레임이 새로 생겨서
이번에 같이 고쳤다. 즉사 분기에서도 빔은 그린다. 딜·판정·`killEnemy` 인자는 그대로고
연출 호출 하나가 뒤에 붙었을 뿐이다.

### 파편 예산 — 동시 개수와 버스트 크기는 다른 문제다

스파크는 처치 파편과 **같은 링 버퍼**를 쓴다. 상한(360)에 닿으면 덮어쓰기가 돌아서
하필 방금 죽은 적의 연출이 빠지므로, **동시** 개수를 시뮬로 잰다. 64판 스윕 최악값이
**스파크 44 / 총 99**(상한의 28%)이고 상한에는 한 번도 안 닿는다. 최악 조합은
관측소 **B2 관통사격**이다 — 한 발이 4마리를 꿰어 발당 16개를 내고, B 분기는 공속이
낮아 표적이 잘 안 죽는다. 스파크는 죽은 적에 안 나므로 **"안 죽는 표적을 여러 번
때리는" 빌드가 최악**이고, 점사(A2)처럼 처치가 연쇄하는 빌드는 오히려 스파크가 적다.

**동시 개수 예산으로는 수명 회귀를 못 잡는다.** 스파크는 발당 4~16개가 한 번에 터지는
버스트이고 버스트끼리 겹치지 않아서, `SPARK_LIFE` 를 0.20 → 0.60(처치 0.3 을 넘겨
이 절의 규칙 자체를 위반)으로 늘려도 동시 개수가 44 → 48 로만 움직인다. 그래서
테스트를 둘로 나눴다 — **동시 피크**가 버스트 크기(`SPARK_N`)를 잡고,
**수명 순서 단언**(화염 < 스파크 < 처치)이 수명을 잡는다. 네 가지 변이
(`SPARK_LIFE` 3배 / `SPARK_N` 4배 / 래치 제거 / `!e.dead` 제거)가 각각
의도한 단언에서 실패하는 것을 확인했다.

처치 파편 몫(동시 57~90)은 2.6 에서 들어온 값이라 이 절의 예산이 아니다.
총 피크가 올랐을 때 어느 쪽이 올랐는지 보이도록 출력만 하고 단언하지 않는다.

---

## 2.8 충격 등급

2.6 이 "죽었다"를, 2.7 이 "타워가 일하고 있다"를 읽히게 만들고 나니 화면 위의
**모든 사건이 같은 무게로 보이는 것**이 남았다. 잡몹 한 마리와 정예 한 마리가
똑같이 사라지고, 박격포 한 발과 빔 한 줄이 똑같이 지나간다. 그래서 사건마다
등급을 매기고 그 등급표를 코드로 잠근다.

| 사건 | 연출 |
|---|---|
| 일반 타격 | 없음 (2.7 의 3박자가 전부다) |
| 오라 · `tick` 지속딜 | 없음 (`!tick` 게이트가 이미 잠그고 있다) |
| 광역 — **박격포 착탄만** | 약한 흔들림 (쿨다운으로 듀티 제한) |
| **일반 처치** | **없음** |
| 정예 처치 | 강한 흔들림 + 히트스톱 |
| 누수 | 화면 층 붉은 비네트만. 흔들림·파편·히트스톱 전부 없음 |

2.7 과 마찬가지로 핵심은 넣는 자리가 아니라 **빼는 자리**다. 일반 처치에 짧은
히트스톱이라도 넣으면 처치가 연쇄하는 후반 구간에서 화면이 초당 수십 번 얼어붙어
배속이 무의미해진다. 정예는 판 전체에 **15마리**뿐이라(w10:1 · w20:3 · w24:3 ·
w28:4 · w30:4) 그 15번만 시간이 멎는다. 광역도 박격포 착탄 하나만 남겼다 —
파쇄자 파열(A1) · 마력로 연쇄(A1) · 서리 장판 · 소이 장판은 전부 발생 빈도가
훨씬 높아서 흔들면 흔들림이 상시가 되고, 상시가 된 흔들림은 정보가 아니다.

### 수치

| 값 | 수치 | 이유 |
|---|---|---|
| `BLAST_SHAKE_AMP` | **0.05 셀** (cell 50px 기준 2.5px) | 타워 표시가 셀의 82%(2.5)라 그 1/16. "칸이 어긋나 보이는" 문턱 아래 |
| `BLAST_SHAKE_DUR` | 0.08초 | 쿨다운과 합쳐 듀티 상한 32% |
| `BLAST_SHAKE_CD` | 0.25초 | 박격포 A(연발) 7성 cd 0.8초 x 다수 + 탄막(A1) 3발 → 초당 최대 4회로 자른다 |
| `KILL_SHAKE_AMP` | **0.14 셀** (약 7px) | 착탄의 2.8배. 두 등급이 눈으로 갈리는 최소 배수 |
| `KILL_SHAKE_DUR` | 0.22초 | 처치 파편 수명 0.3초(2.6) 안에서 끝난다 — 파편이 사라진 뒤 흔들림만 남으면 원인이 안 읽힌다 |
| `HITSTOP` | 0.05초 | 60Hz 3프레임 / 30Hz 2프레임. 2.7 과 같은 이유로 프레임 수가 아니라 초로 적는다 |
| `LEAK_WARN_DUR` / `LEAK_WARN_A` | 0.35초 / 알파 0.22 | 비네트. 알파는 누적하지 않는다 |
| 흔들림 주파수 | `62 rad/s` · `71 rad/s` | 두 축 주기가 서로소에 가까워 패턴이 안 반복된다. 같은 값이면 대각선 왕복으로만 보인다 |

**진폭은 픽셀이 아니라 셀 단위다.** 픽셀로 저장하면 화면 회전·리사이즈로 `view.cell`
이 바뀌는 순간 진폭만 옛 화면 크기에 남는다. 2.6 이 파편 좌표에서 겪은 것과 같다.

### 히트스톱은 `dt` 를 안 건드린다 — `update()` 를 통째로 건너뛴다

```
if (hitstopT > 0) hitstopT -= dt;          // update 도, 흔들림 감쇠도 건너뛴다
else { decayShake(dt); leakWarnT -= dt; for (i<steps) update(dt); }
render();                                   // 히트스톱 프레임에도 반드시 부른다
```

`dt` 를 곱하거나 나누면 `update()` 가 보는 스텝 폭이 달라져서 **판정이 연출에
끌려간다.** 2.7 의 "배속은 dt 를 늘리는 대신 update 를 여러 번 돈다"와 같은 규칙이다.
`update()` 가 받는 `dt` 는 이 절의 전후로 비트 단위 동일하다.

이 분기는 **`frame()` 안에만 있다.** `tools/sim.js` 의 `EXPOSE` 에 `frame` 이 없고
그리디 플레이어는 `update(1/30)` 을 직접 돌리므로, 히트스톱은 시뮬·테스트에
원리적으로 못 샌다. 테스트는 `hitstopT` 를 0.5 로 강제한 판과 0 인 판을 각각
200스텝 돌려 결과 요약이 **문자열까지 같은 것**으로 이걸 잠근다.

`render()` 는 히트스톱 프레임에도 부른다. 여기서 early-return 하면 `render` 호출
수로 프레임을 세는 `verify-build` 가 두 페이지에서 다른 지점을 찍는다.

### 흔들리는 것은 카메라지 판이 아니다

흔들림은 `render()` 의 `ctx.save()` 직후, **보드 클립보다 앞**에서 `translate` 한
줄로 낸다. 클립 뒤에 두면 고정된 창 안에서 내용만 밀려 한쪽 가장자리에 `#0d1117`
틈이 생긴다. 창째로 움직여야 보드가 흔들린 것으로 읽힌다.

**`view` · `cellToPx` · `pxToCell` 은 한 글자도 안 고친다.** 터치 히트박스는
`view.ox/oy/cell` 만 읽으므로, 이 셋이 안 움직이는 것이 흔들리는 동안에도 손가락이
안 어긋나는 **유일한 근거**다. `view` 를 밀어서 흔드는 구현은 화면상 똑같이 보이면서
히트박스만 조용히 어긋난다 — 테스트가 보드 전 칸의 중심과 경계 ±1px 을 흔들림
최대 진폭에서 다시 찍어 이걸 잠근다.

클립 밖에 있는 것들(드래그 중인 타워 · HUD · 튜토리얼 · 정보창 · 소환 피커 ·
정지 화면 · 종료 오버레이 · 분기 모달)은 **안 흔들린다.** 착탄 순간에 피커가 열려
있어도 피커의 딤과 셀 테두리는 제자리다 — 의도된 동작이다.

### 오프셋은 순수 함수다 — 난수를 안 쓴다

`shakeOffset()` 은 `shake` 상태만 보는 순수 함수다. `fxRand()` 조차 안 쓴다.
매 렌더마다 `fxSeed` 를 밀면 `verify:build` 의 두 페이지가 프레임 수 차이 한 번에
갈리고(2.6 "연출은 전역 난수를 쓰지 않는다"의 같은 함정), 테스트가 오프셋 값을
단언할 방법도 없어진다.

### 합치기는 합산이 아니라 최댓값이다

탄막(A1)은 한 번의 발사로 3발이 같은 프레임에 착탄하고, 정예는 장판에 여러 마리가
한꺼번에 죽는다. 합산하면 착탄 3회가 정예 처치보다 세져서 **그 자리에서 등급표가
뒤집힌다.** 진행 중인 흔들림보다 약한 것은 덮지도 않는다(남은 진폭과 비교).
히트스톱도 `Math.max` 다 — 정예 4마리가 같이 죽어도 멎는 시간은 1회분이다.

### 누수 경고가 2.6 과 양립하는 방식

2.6 이 금지한 것은 **보드 층**이다 — 파편(파티클), 셀 좌표, `killEnemy` 경로.
누수 경고는 그 셋을 하나도 안 쓴다. ① 파티클 0개 ② 흔들림 0 · 히트스톱 0
③ 화면 가장자리 붉은 비네트만. 처치가 "보드 위 그 자리에서 터지는 밝은 파편"이라면
누수는 "화면 가장자리가 붉어지는" 정반대 결이라 둘이 안 헷갈린다.

`leakWarnT` 는 `+=` 가 아니라 `=` 다. 누수가 몰리는 구간(w21 붕괴)에서는 비네트가
사실상 상시인데, 그건 정확한 표현이라 그대로 둔다. 다만 알파가 누적되면 판이
안 보이므로 진하기는 `LEAK_WARN_A` 를 절대 안 넘는다.

### 흔들림은 끌 수 있다 — 히트스톱은 이 스위치가 안 끈다

정지 화면(= 이 게임의 유일한 설명서)에 토글 하나를 둔다. 자리는 **제목·부제 다음,
절 루프 앞**이다. 절 안에 넣으면 "안 들어가면 뒤에서부터 버린다" 로직에 걸려
하필 작은 화면 — 흔들림이 제일 거슬리는 기기 — 에서 사라진다.

| 항목 | 값 |
|---|---|
| 저장 | `localStorage['cant-hold-shake']` **단독 키**. 세이브 번들·클라우드 동기화에 안 넣는다 (`cant-hold-tute` 와 같은 형태). `SAVE_VERSION` 은 안 올린다 |
| 기본값 | 저장값이 없으면 `prefers-reduced-motion` 의 반대 |
| 끄면 | 진행 중이던 흔들림도 즉시 죽는다. 안 그러면 스위치가 안 먹은 것처럼 보인다 |
| 탭 | 토글을 누르면 정지가 유지된다. 같이 재개하면 켜졌는지 확인할 화면이 사라진다 |

**히트스톱은 이 토글이 안 끈다.** 멀미의 원인은 카메라 운동이고, 히트스톱은
판당 15회짜리 시간 정지다. 둘을 한 스위치에 묶으면 "화면 흔들림"이라는 라벨이
거짓말이 된다.

### 판을 갈아끼울 때

`togglePause()` · `restart()` · `restoreRun()` 에서 `shake` · `shakeCd` ·
`hitstopT` · `leakWarnT` 를 전부 0 으로 되돌린다(`resetImpact()`). 흔들리는 도중에
멈추면 진폭이 얼어붙었다가 재개하는 순간 남은 만큼 튀어 오른다. 2.6 이 파편에서
"판을 갈아끼우는 함수는 어느 경로로 빠져나가든 옛 판의 잔여물을 안 남긴다"로
정한 것과 같은 규칙이다.

이 상태들은 **`state` 가 아니라 모듈 레벨**이다. `state` 에 넣으면 `snapshotRun`
감시 대상이 되어 `SAVE_VERSION` 논의가 딸려 온다 — 이건 판의 진행이 아니라
카메라 이야기라 세이브에 실릴 값이 아니다.

### 눈으로 확인하는 법

흔들림은 0.22초짜리고 감쇠는 `frame()` 이 돌기 때문에, `tools/shot.js` 처럼
`window.update` 만 덮는 방식으로는 **반드시 0 이 된 프레임이 찍힌다.** 그래서
`7-shake` 컷은 `update` 를 "매 프레임 최대 진폭으로 흔들림을 되살리는 스텁"으로
덮고 찍는다. 볼 것은 하나다 — **보드만 밀리고 상단 HUD·하단 버튼은 제자리인가.**

---

## 2.9 사운드

2.8 이 사건마다 등급을 매겼는데 그 등급이 **눈에만** 있었다. 화면에서 시선을 떼면
아무 일도 안 일어난 것과 같다. 그래서 같은 등급표를 귀로 한 벌 더 낸다. 등급표를
새로 만들지 않는 것이 이 절의 첫 규칙이다 — 두 벌이면 조용히 갈린다.

### 세 가지 규칙이 전부다

| 규칙 | 안 지키면 |
|---|---|
| **지연 개방** — 로드 시점에 `AudioContext` 를 안 만든다. `pointerdown` 의 `sfxUnlock()` 이 유일한 개방점 | 제스처 전에 만든 컨텍스트는 `suspended` 로 태어나 굳는다. 헤드리스에는 `AudioContext` 가 아예 없어서 로드가 터진다 |
| **시계는 오디오 시계** — 모든 쿨다운이 `actx.currentTime` | 게임 `dt` 로 재면 배속 x4 에서 쿨다운도 4배 빨리 풀리고, 히트스톱 프레임에는 `dt` 누적이 통째로 멈춘다 |
| **난수는 전용 스트림** (`sfxRand`, `fxRand` 와 별개 시드) | 음소거·미개방일 때 `sfx()` 가 조기 반환하므로 소리를 낸 판과 안 낸 판에서 `fxSeed` 가 갈리고, 파티클 각도가 달라져 `verify:build` 의 두 페이지가 다른 그림을 그린다. 전역 `Math.random` 은 2.6 과 같은 이유로 금지 |

### 게이트 순서가 곧 명세다

```
① 컨텍스트 없음 · 음소거      → return
② 쿨다운 (오디오 시계)         → dropped++
③ 보이스 상한 (8)              → dropped++
④ nextAt / played / 마스킹 갱신
⑤ play(now, 피치)
```

**상한에 닿으면 드롭한다.** 2.6 의 파티클이 "막지 말고 덮어쓴다"인 것과 정반대인데,
이유가 정반대라서 그렇다 — 파편은 안 보이면 정보가 사라지지만 **소리는 겹칠수록
나빠져서 드롭이 곧 개선**이다.

### 겹침 수치

| 큐 | 쿨다운 | 길이 | 통과(피크) | 듀티 |
|---|---|---|---|---|
| `shot` | 0.06s | 0.035s | 10.5회/초 (입력 21발/초) | 58% |
| `kill` | 0.14s | 0.09s | 6.7회/초 (입력 g=27~33ms) | 64% |
| `killBig` | 0.20s | 0.30s | 판당 15회 | — |
| `blast` | `shakeCd` 0.25s 공유 | 0.18s | 탄막 3발 → 1회 | 72% |
| `leak` | 0.35s | 0.24s | 2.9회/초 | 69% |

- **`shot` 0.06s 근거**: 20Hz 를 넘기면 귀가 개별 사건이 아니라 하나의 톤(버즈)으로
  융합한다 → 16.7Hz 로 그 아래에 둔다. 통상 구간 상한(9발/초=111ms)의 절반보다
  작아서 **x1 통상 플레이 드롭 0**
- **반복 큐는 길이가 쿨다운보다 짧다.** 듀티가 100% 를 넘으면 자기 꼬리와 겹쳐서
  그 자체로 톤이 된다. 예외 둘은 이유가 다르다 — `blast` 는 자체 쿨다운이 없고
  `shakeCd`(0.25) 를 공유해서 실효 듀티가 72%, `killBig` 은 판당 15회뿐이라
  자기 꼬리와 겹칠 일이 없어서 `cd` 를 밀도 제한이 아니라 "정예가 연달아 죽을 때의
  최소 간격"으로 쓴다(정예 4마리가 장판에 같이 죽는 w30 에서 최대 0.10s 겹침)
- **`blast` 는 자체 쿨다운을 안 둔다** — `updateShells` 의 `shakeCd` 게이트 안에
  `sfx('blast')` 가 들어간다. 게이트를 공유하면 **보이는 흔들림과 들리는 쿵이 항상
  1:1** 이라 "소리는 났는데 안 흔들림"이 원리적으로 없다. `BLAST_SHAKE_CD` 를
  소리 때문에 고치면 안 된다 — 그 값은 2.8 의 흔들림 듀티 근거다
- **`leak` 만 피치 스프레드 0** — 경고음은 매번 같아야 경고로 읽힌다. 나머지는
  ±3~6%(반음=5.95%)
- 최악 동시 보이스 ≈ 4.0. `SFX_VOICE_CAP = 8` 은 정상 플레이에서 안 닿고
  폭주 시에만 문 역할을 한다

### 음색

전부 `createOscillator` + `createGain`. 파일을 안 쓰는 이유는 2.5 와 같다 —
받는 시간이 없고 고칠 때 값만 만지면 된다. 엔벨로프는 `setValueAtTime` →
`linearRampToValueAtTime`(attack) → `exponentialRampToValueAtTime`(decay).
**감쇠 목표는 0.0001 이다 — 정확히 0 으로 램프하면 예외가 난다.**

| 큐 | 파형 | 주파수 | Attack | Decay | 피크 게인 |
|---|---|---|---|---|---|
| `shot` | `square` | 1800 → 900Hz (0.030s exp) | 0.002s | 0.033s | 0.09 |
| `blast` ①탁 | `square` | 220Hz 고정 | 0.001s | 0.028s | 0.06 |
| `blast` ②쿵 | `sine` | 150 → 70Hz (0.16s exp) | 0.004s | 0.176s | 0.30 |
| `kill` | `triangle` | 520 → 260Hz (0.075s exp) | 0.001s | 0.088s | 0.07 |
| `killBig` ①본체 | `triangle` | 260 → 90Hz (0.26s exp) | 0.002s | 0.298s | 0.16 |
| `killBig` ②배음 | `square` | 130Hz 고정 | 0.002s | 0.055s | 0.05 |
| `leak` | `sawtooth` | 200 → 130Hz (0.22s exp) | **0.010s** | 0.230s | 0.13 |

- **파형이 큐마다 다른 게 핵심이다.** 같은 파형에 주파수만 다르면 겹쳤을 때
  무엇이 무엇인지 안 갈린다
- **하강 처프로 통일.** 상승은 "얻음"으로 읽혀서 처치·발사에 안 맞는다
- **`leak` 만 attack 10ms**(다른 큐의 5~10배). 느린 어택은 "타격"이 아니라
  "상태"로 읽힌다. `sawtooth` 는 거칠어서 불쾌한데 누수는 불쾌해야 한다
- **`blast` 저역 하한 70Hz.** 45Hz 까지 떨어뜨리면 폰 스피커에서 뒷부분이 통째로
  사라진다. 220Hz `square` 층은 missing fundamental 로 저음을 암시하는 장치다
- `SFX_MASTER = 0.6` + `DynamicsCompressorNode` 하나. 8보이스 x 피크 게인 합이
  1.0 을 넘어서 컴프레서 없이는 클리핑으로 찢어진다

### 등급 마스킹

`killBig` 이 실제로 재생되면 `nextAt.kill = now + 0.30`. 정예 처치 순간에 잡몹
처치음이 얹히면 2.8 이 흔들림으로 갈라 놓은 등급이 귀에서 도로 뭉친다.
판당 15회뿐이라 비용이 없다.

### 어디에 넣고 어디에 안 넣는가

2.7 과 마찬가지로 핵심은 넣는 곳이 아니라 **빼는 곳**이고, 빼는 자리는 전부
**구조로** 잠갔다 — 호출부 플래그는 규칙이 문법적으로 보이는 몇 줄에서만 지켜진다.

| 위치 | 소리 |
|---|---|
| `muzzle()` 함수 끝 | `sfx('shot')`. 2.7 의 총구 화염과 **같은 자리**다 |
| `updateShells` 의 `shakeCd` 게이트 안 | `sfx('blast')` |
| `killEnemy` 의 정예 분기 | `sfx('killBig')` |
| `killEnemy` 의 일반 처치 경로 | `sfx('kill')` |
| `state.life -= d.leak` 아래 (`leakWarnT` 옆) | `sfx('leak')` |
| `pointerdown` 첫 줄 (`cloudPreload()` 옆) | `sfxUnlock()` |
| **서리탑 · 조폐소 · 오라 · 장판 · `tick` 딜** | **없음** — `muzzle()` 을 안 지난다. 2.7 이 화염에서 얻은 제외가 그대로 물려진다 |
| **파쇄자 파열(A1) · 마력로 연쇄(A1) · 소이/동상 장판** | **없음** (같은 이유) |
| **`beam()` / `spawnSparks()`** | **없음** |
| 즉사 2경로(관측소 B1 · 7성 처형) | `killEnemy` 경유 `sfx('kill')`. 둘 다 `!elite` 조건이 있어 `killBig` 은 안 난다 |
| 배치 · 합성 · 웨이브 시작 · UI 탭 · 타워 7종 구분 | **없음** — 다음 티켓 |

**"착탄"은 박격포만이다.** 히트스캔(파쇄자·침식자·관측소)은 `muzzle()` 과 `beam()` 이
**같은 프레임**이라 둘 다 틀면 시간차 0 으로 뭉개지고 초당 요청이 21 → 42 로 뛴다.
비행 0.5초짜리 박격포만 시간축에서 갈린다.

**일반 처치에는 소리를 넣는다** — 2.8 이 일반 처치를 뺀 근거는 "초당 수십 번
얼어붙으면 배속이 무의미해진다"로 비용이 **시간축**에 있었다. 소리에는 그 비용이 없고
밀도는 쿨다운이 6.7회/초로 자른다. 등급은 `kill`/`killBig` 두 큐 + 마스킹으로 유지된다.

### 음소거 토글

흔들림 토글과 같은 형태다. 정지 화면의 `{t:'shake'}` **바로 다음** 줄이고,
절 루프 앞이라 작은 화면에서 안 잘린다(320x568 에서 확인).

| 항목 | 값 |
|---|---|
| 저장 | `localStorage['cant-hold-sound']` **단독 키**. 세이브 번들·클라우드 제외. `SAVE_VERSION` 안 올린다 |
| 기본값 | **켜짐.** 대응할 미디어 쿼리가 없고, 첫 터치 전에는 `AudioContext` 가 안 열려서 예고 없이 소리가 날 창이 원리적으로 없다 |
| 끄면 | `master.gain` 을 0.02초 램프로 0 까지. 즉시 0 을 대입하면 울리던 소리가 딸깍 끊긴다. 이후 `sfx()` 는 ①번 게이트에서 조기 반환하므로 노드를 아예 안 만든다 |
| 정지 중 | 별도 처리 없음. `update()` 가 `state.paused` 에서 return 하므로 새 사건이 안 난다. **`resetImpact()` 에 소리를 넣지 않는다** |

### 엣지 케이스

- **iOS 는 `AudioContext` 가 `suspended` 로 태어난다** → `sfxUnlock()` 에서 `resume()`
- **탭 전환 후 복귀** → `sfxUnlock()` 은 **멱등**이고 `pointerdown` 마다 불린다.
  첫 터치 전용으로 만들면 백그라운드에서 내려간 컨텍스트가 안 살아나 그 뒤로
  소리가 영영 안 난다
- **한 프레임에 20마리 처치(장판)** → 오디오 시계가 같으므로 쿨다운이 1회만
  통과시킨다. **20마리가 죽어도 소리는 1번**이 의도된 동작이다
- **`onended` 가 안 오는 경우**(컨텍스트 close, 헤드리스 스텁) → `voices` 가 영영
  안 줄면 상한에 막혀 소리가 통째로 죽는다. `stop()` 예약 시각 + 0.10초를 지나면
  강제로 회수한다(`SFX_REAP_SLACK`). `onended` 가 정상으로 오면 그쪽이 먼저 줄인다
- **`localStorage` 접근 실패**(사파리 프라이빗) → `shakeEnabled` 와 같이 `try/catch`
- **구형 사파리** → `window.AudioContext || window.webkitAudioContext`

### 눈으로 확인할 방법이 없다 — 그래서 카운터로 잠갔다

소리는 헤드리스에서 못 듣고 `verify:build` 도 못 잡는다. **`tools/verify-build.mjs`
는 `click`/`tap`/`pointerdown`/`dispatchEvent` 를 한 번도 안 보내서 오디오를 아예
안 연다.** 그래서 검사 창을 게임 코드 안에 둔다(2.8 이 `hitstopState()` 게터를
게임 코드에 둔 것과 같은 전례).

1. `sfxStats()` 가 큐별 `played`/`dropped`/`voices`/`open`/`enabled` 를 낸다
2. `tools/sim.js` 의 `window` 스텁에 **`AudioContext` 스텁**을 둔다. 하는 일은
   **시계를 들고 있는 것**뿐이다 — `api.audio.advance(s)` 로 테스트가 오디오 시계를
   앞으로 감는다(`api.draws` 와 같은 형태)
3. **밸런스 경로는 영향 0.** `sfxUnlock()` 은 `pointerdown` 에서만 불리고
   `canvas.addEventListener` 는 no-op 이라, 스텁이 있어도 `greedy`/`tune`/`seedcheck`
   는 `actx` 가 `null` 인 채로 돈다. 테스트만 `g.sfxUnlock()` 을 명시적으로 부른다
4. **`fxState()`** 로 `fxSeed` 를 직접 잰다. 사운드가 `fxRand()` 를 쓰는 회귀는
   게임 상태에 아무 흔적을 안 남기고 그림 차이로만 나타나는데, 위 이유로 어느
   하네스도 그걸 못 본다 — 실제로 `sfxRand()` → `fxRand()` 변이가 이 단언 하나에만
   걸린다

여섯 가지 변이(발사음 훅 제거 / 쿨다운 게이트 제거 / 보이스 상한 제거 /
`blast` 훅 제거 / 마스킹 제거 / `sfxRand` → `fxRand`)가 각각 의도한 단언에서
실패하는 것을 확인했다.

### 파일로 갈아탈 때 무엇이 버려지나

지금은 합성음이라 **음색 자체가 코드**다. `.wav`/`.mp3` 로 갈아타면 다음이 없어진다.

| 버려지는 것 | 지금 무엇을 하고 있나 |
|---|---|
| **피치 스프레드** | `play(t, rate)` 의 `rate` 가 주파수를 통째로 민다. 파일이면 `playbackRate` 로 흉내낼 수 있지만 길이도 같이 변해서 위의 듀티 표가 깨진다 |
| **엔벨로프 표** | attack/decay 가 값 한 줄이다. 파일이면 편집기로 다시 굽는 작업이 되고, "왜 `leak` 만 10ms 인가" 같은 근거가 코드에서 사라진다 |
| **0바이트 · 0요청** | 지금은 받을 것이 없다. 파일은 첫 재생 전에 디코드가 끝나 있어야 해서 프리로드와 "아직 안 받아졌을 때" 분기가 새로 생긴다 |
| **`build.mjs` 무개입** | 이 리포의 배포물은 `index.html` 한 장이다. 파일이 생기면 자산 목록·캐시 정책이 딸려 온다 |

반대로 파일이 이기는 지점은 **음색의 표현력** 하나다. 합성음으로는 타워 7종을
음색으로 구분하는 것(다음 티켓 후보)이 사실상 안 된다 — 파형이 4종뿐이라
이미 5큐에 다 썼다. 그 티켓을 할 때가 갈아탈 시점이다.

---

## 2.10 2x2 결과 자리 고르기

5성 이상 합성 결과는 2x2 인데 그 자리를 `mergeSpot()` 이 혼자 정하고 있었다.
놓은 쪽(`b`) → 집은 쪽(`a`) 순으로 좌상 오프셋을 훑고, 둘 다 안 되면 개방된 행의
좌상단부터 스캔해 **처음 걸리는 칸**에 박는다. 판에서 제일 비싼 타워가 플레이어가
고른 적 없는 자리 — 대개 경로에서 제일 먼 구석 — 에 생겼다. 스테이지 2·4의 커버
편차가 1.87 이라는 것은 "어디에 놓느냐가 곧 성능"이라는 뜻인데, 그 결정만 게임이
대신 내리고 있었다.

**5성만의 문제가 아니다.** `mergeResultSize` 가 `star >= 5 && kind !== 'mint'` 이므로
6·7성 진입도 전부 2x2 다. 5성으로 좁히면 같은 스트레스가 6성에 그대로 남는다.

**1x1 결과는 안 묻는다.** `occ` 가 부모 둘을 빼고 세므로 `b.gx,b.gy` 는 항상 비어 있고,
`mergeSpot` 의 첫 검사가 반드시 통과한다 — 결과는 늘 손을 뗀 칸이고 물을 것이 없다.

### 커밋하지 않는 것이 이 설계의 전부다

모드를 여는 `beginMergePlace()` 는 **골드를 안 깎고 부모 둘을 안 지운다.** id 두 개와
후보 목록만 들고 있다가 `mergePlaceCommit()` 에서 처음으로 `mergeTowers()` 를 부른다.

| 만약 먼저 합쳐 놓고 물었다면 | 지금 |
|---|---|
| 취소가 "되돌리기" 가 된다 — 골드·부모 둘·`nextTowerId`·`pushChoice` 큐·물려받은 분기를 전부 되감아야 한다 | 취소가 **무동작**이다 |
| 웨이브가 끼어들면 되감기 도중 상태가 남는다 | `state.picker = null` 한 줄 |
| 세이브(`snapshotRun`)가 "합쳐졌지만 자리가 없는 타워" 를 저장할 수 있다 | 저장할 것이 없다. `SAVE_VERSION` 안 올린다 |

그래서 **배치를 다 시켜 놓고 마지막에 거절하는 경로가 없어야 한다.** `mergeAllowed` ·
`canMerge` · 비용 · 후보 0곳을 전부 `beginMergePlace()` **앞단**에서 보고, 하나라도
막히면 모드를 안 열고 지금까지와 같은 토스트를 낸다.

### 새 상태를 안 만든다 — `state.picker` 에 얹는다

`state.picker = { mode: 'summon' | 'mergePlace', ... }`. 형제 상태(`state.mergePlace`)를
만들지 않는 이유는 **정리 경로가 이미 넷**이기 때문이다 — `togglePause()` ·
`restoreRun()` · `restart()` · ESC. 새 상태를 만들면 그 넷을 전부 다시 맞춰야 하고,
하나만 빠뜨리면 "일시정지했다 풀면 놓은 적 없는 합성이 성사된다" 가 난다.
2.7 이 `beam()` 에서 겪은 "규칙이 문법적으로 보이는 몇 줄에서만 지켜진다" 와 같은
함정이다. `drawTutorial()` 이 피커가 열려 있으면 안 그리는 것도 공짜로 따라온다.

### `mergeSpot` 은 한 글자도 안 고친다

후보 **전부**를 내는 `mergeSpots()` 를 따로 둔다. `mergeSpot` 은 계속 두 곳에서 쓰인다 —
1x1 경로, 그리고 배치 모드의 **기본 선택(`sel`)**. 생각 없이 `배치` 를 누르면 오늘까지와
같은 결과가 나온다는 뜻이고, 이것이 1x1 시드 회귀(`npm run seedcheck` 의 결과 문자열과
**난수 호출 횟수**)가 안 흔들리는 근거다.

불변식 하나로 잠근다: **`mergeSpot(a,b,s) !== null` ⟺ `mergeSpots(a,b,s).length > 0`**.
같은 `occupancy(a,b 제외)` 와 같은 `canPlace` 를 보므로 성립한다. 깨지면 "미리보기는
자리가 있다는데 모드가 안 열린다" 가 난다. 4스테이지 x 무작위 배치 100판에서 반례 0.

`mergeTowers(a, b, at)` 는 `at` 이 와도 `canPlace` 로 **다시 본다.** 호출부를 믿으면
모드가 열려 있는 사이에 판이 바뀐 경우(행 개방·다른 배치) 겹쳐 놓인 타워가 조용히 생긴다.

### 소환 피커와 두 곳이 다르다

| | 소환 | 배치 |
|---|---|---|
| 딤 | 전면 `0.78` | **보드 위 `0.26`** · 보드 밖 `0.78` |
| 카드 | 덱 3장 | 없음 (무엇을 놓을지는 이미 정해졌다) |
| 보드 탭 | 무시 → 닫힘 | 후보 칸이면 `sel` 이동, 밖이면 무시 |
| 바깥 탭 | 닫힘 | **안 닫힌다** |
| 액션 | `취소`/`배치` | `취소`/`배치` (같은 `PICKER_ACT_H`) |

**딤이 다른 이유가 이 화면의 핵심이다.** 고를 대상이 후보 칸 자체라, 소환의 `0.78` 을
그대로 쓰면 골라야 할 것이 통째로 묻힌다. **알파는 헤드리스가 못 잡는다** — `npm run shot`
의 `8-mergeplace` 컷을 눈으로 보고 판정한다. 볼 것은 하나다: **후보 칸과 보드가 딤에
안 묻히는가.**

**바깥 탭에 안 닫히는 이유**: 배치 모드에서는 보드가 통째로 "바깥"이라, 빗나간 탭
한 번에 합성이 날아간다. 취소는 버튼으로만 한다.

`sel` 자리의 2x2 점선+십자선은 `drawMergeGhost()` 하나를 **드래그 미리보기와 공유**한다.
놓기 전에 본 네 칸과 놓은 뒤에 고르는 네 칸이 다르게 생기면 미리보기가 거짓말이 된다.
범위 사각형은 후보 칸보다 **먼저** 그린다 — 관측소처럼 넓은 종류는 범위가 보드 절반을
덮어서, 순서가 뒤집히면 옅은 후보 칸이 통째로 씻겨 나간다.

부모 둘의 분기가 다르면 사거리가 아직 확정이 아니다(물려받기를 안 물었다).
**놓은 쪽(`b`)의 분기로 원 하나만** 그리고 `분기는 배치 뒤에 고른다` 를 붙인다.
두 원을 겹쳐 그리지 않는다 — 3.1 과 같은 이유로 겹친 원은 누구 건지 안 읽힌다.

**흔들림은 안 탄다.** 2.8 이 "클립 밖(소환 피커 포함)은 안 흔들린다" 로 정한 그대로다.
보드 층으로 내리면 딤·후보·유령·범위를 전부 `translate` 안으로 옮기면서 소환 피커와
층이 갈린다.

**웨이브 중에도 이 화면이 열리므로 이제 흔들림원이 실제로 있다** — 뒤에서 적이 터지고
보드가 흔들리는 동안 자리를 고른다. 그래도 규칙은 그대로다: 이 화면은 클립 밖이라 안
흔들리고 흔들리는 것은 그 뒤의 보드다. 고를 대상(후보 칸)이 보드 위에 있으므로
**딤이 옅은 것이 여기서 값을 한다** — 흔들리는 판 위에서도 후보 칸이 읽혀야 한다.

### 게임 시계를 안 멈춘다 — 웨이브가 와도 모드는 살아 있다

멈추면 모드를 열고 닫는 것만으로 준비 시간을 무한히 늘릴 수 있다(README: "시간 압박이
이 게임의 난이도 그 자체"). 그래서 웨이브는 그냥 온다.

**예전에는 `startWave()` 첫 줄에서 모드를 지우고 토스트를 냈다.** 이유는 `mergeAllowed`
가 준비 단계 전용이라 웨이브로 끌고 가면 배치를 다 시켜 놓고 마지막에 거절하는 화면이
되기 때문이었다. **합성이 웨이브 중에도 되면서 그 이유가 사라졌다** — 이제 끌고 간다.

고르는 동안 적이 흐르는 것은 그대로다. 그게 손해가 아니라 **선택의 값이다** — 전투
중에 합칠지, 한 박자 기다릴지. 테스트가 "웨이브가 시작돼도 모드가 살아 있다" 와
"웨이브 중에 배치를 확정할 수 있다" 로 이걸 잠근다.

### 밀어내기는 안 한다

자리가 없을 때 다른 타워를 자동으로 밀면, 밀려난 타워가 다시 "내가 안 고른 자리" 로
가서 불만이 그대로 옮겨간다. 드래그 재배치는 별도 티켓이다.

### 난이도 상한

자리를 고를 수 있게 되면 게임이 쉬워진다. 얼마나 쉬워지는지는 **`mergeSpot` 을 "경로
커버 칸 수가 최대인 후보" 로 갈아끼운 greedy** 와 현재 greedy 의 평균 사망 웨이브
차이로 잰다(35덱 x 8회, 클리어는 `parity.js` 와 같이 30 으로 자름).

| 스테이지 | 현재 | 커버 최대 | 차 |
|---|---|---|---|
| 2 (이중 병목, 커버 편차 1.87) | 23.97 | 26.96 | **+2.99** |
| 4 (역류, 2레인) | 27.75 | 28.04 | +0.29 |

**2스테이지가 기준선(+1.5)을 넘는다.** 커버 편차가 큰 판일수록 자리 선택의 값이 커지므로
방향은 예상대로지만 폭이 예상보다 크다. 이건 **상한**이다 — greedy 는 커버 칸 수만 보고
항상 최적을 고르는 플레이어이고, 실제 플레이는 이보다 낮다. 이 티켓에서 밸런스를 만지지
않는다(수치를 건드리면 2.x 의 근거들이 통째로 흔들린다). **별도 밸런스 티켓**이다.

---

## 3. 타워 7종

일하는 방식으로 셋으로 갈린다. 고를 때 이게 안 보이면 오라 타워를
사거리 짧은 딜러로 착각한다. 덱 선택 화면은 이 그룹으로 나눠 보여준다.

| 그룹 | 타워 | 성격 |
|---|---|---|
| **오라** | 파쇄자 · 침식자 · 서리탑 | 주변에 상시로 깔린다. 어디에 놓느냐가 성능 |
| **공격** | 박격포 · 관측소 · 마력로 | 사거리 안의 적을 직접 때린다 |
| **경제** | 조폐소 | 공격하지 않는다. 골드만 번다 |

### 3.1 파쇄자 — 방어력 감소 / 물리

```
base 0.6   공속 0.6s   사거리 2.4   물리
오라: 사거리 안의 적에게 초당 1.5스택, 스택당 방어력 -1.2
      상한 = 성급 x 3, 오라를 벗어나면 초당 3씩 빠진다
      본인 딜도 깎인 방어력 혜택을 그대로 받음
      + 물리 취약 (0.03 + 0.02 x 성급) x SHRED_VULN — 5성 +39%
```

#### 방깎만으로는 적 절반에게 아무 일도 안 일어난다

적 8종의 방어력은 `보병 5 · 중갑 40 · 마갑 5 · 질주 0 · 재생 8 · 면역 12 · 군집 0 ·
정예 25` 로 **절반이 0~5 다.** 스택당 −1.2 를 아무리 쌓아도 0 은 0 이라, 파쇄자 오라는
질주·군집·보병·마갑 앞에서 사실상 꺼져 있었다. 기여도(§2 대등성) −2.7 로 7종 최하위였던
이유가 이것이다.

**침식자가 같은 구멍을 이미 풀어 놨고 파쇄자만 안 풀렸다.** 침식자 코드 주석:
"마방 감소만으로는 마방 0인 적(8종 중 4종)에게 아무 일도 안 일어난다. 감소는 마갑
카운터로 남기고, 범용 가치는 마법 취약으로 따로 준다." 침식자는 `mresCut`(카운터)과
`magicVuln`(범용) 둘을 쌓는데 파쇄자는 `armorStacks` 하나만 쌓고 있었다. 그 대칭을 채운다 —
**방깎은 중갑·정예 카운터로 남기고, 물리 취약(`physVuln`)을 범용 가치로 따로 준다.**

**속성 상성이 아니다**(§7). 새 축을 만든 게 아니라 이미 있는 `ampMagic` 의 짝
(`ampPhys`)을 채운 것이고, 딜 타입은 여전히 물리/마법/순수 3종뿐이다. 적용 지점도
`damage()` 의 물리 분기 한 곳이고, §6.1 의 증폭 가산 규칙을 그대로 따른다
(`ampAll` 과 곱해지는 층은 `ampMagic` 과 동일한 자리).

**세기는 손으로 안 맞췄다.** `CFG.SHRED_VULN` 을 두고 `tools/parity.js` 로 스윕했다
(A/A1, 56덱 x 7시행, 시드 12345 — `npm test` 와 같은 조건):

| SHRED_VULN | 기여도 폭 | 파쇄 | 최대 &#124;기여도&#124; |
|---|---|---|---|
| 0 (없음) | 6.65 | −4.32 | 4.32 |
| 1 (침식자와 동일) | 4.87 | −2.21 | 2.66 |
| 2 | 4.52 | −1.16 | 2.33 |
| **3 (채택)** | **4.22** | **−0.50** | **2.33** |
| 4 | 4.90 | +1.07 | 2.79 |

3 에서 폭이 최소이고 파쇄자 기여도가 0 에 가장 가깝다. 4 는 파쇄자를 양수로 밀어
이번엔 박격포를 −2.79 로 끌어내린다. 분기 배수는 안 붙인다 — 분기는 스택당 방깎·상한·
범위에서 이미 갈리고, 이 값은 그 분기와 무관하게 "절반의 적에게 무효"를 메우는 자리다.

화면 표시는 **주황 막대 그대로다.** 물리 취약은 스택이 아니라 오라 접촉 자체로 걸리므로,
막대가 떠 있다는 것이 곧 "오라가 닿았다"는 표시다(취약 수치를 따로 그리지 않는다).

**때려서 거는 게 아니라 주변에 상시로 깔린다.** 배치 위치가 곧 디버프 범위라서
어디에 놓느냐가 성능이 된다. 그래서 오라 범위는 화면에 그려야 한다 —
사거리를 숫자로만 적으면 어디까지 닿는지 아무도 모른다. 오라는 실선 + 진한 채움,
사격 사거리는 점선으로 구분한다.

**디버프는 적 몸에 표시된다.** 수치가 안 보이면 디버프 타워가 일하는지 알 수 없다.

| 디버프 | 표시 |
|---|---|
| 슬로우 | 적 색이 서리색으로 물든다. 강도에 비례해 농도가 달라진다 |
| 방어력 감소 | 적 아래 **주황 막대**. 상한 대비 현재 스택 |
| 마방 감소 + 마법 취약 | 적 아래 **보라 막대** |
| 빙결 | 적이 통째로 서리색 |

방깎 스택은 오라를 벗어나면 **초당 3씩 빠진다.** 예전에는 4초 뒤 한 번에 0 이었는데,
그러면 언제 풀렸는지 알 수 없고 막대가 뚝 사라진다.

**오라 범위는 선택하지 않아도 항상 옅게 깔아 둔다.** 오라는 눈에 안 보이면 없는 것과 같고,
적이 그 안에 들어온 순간이 그대로 읽혀야 한다. 슬로우 걸린 적은 **몸 색이 서리색 쪽으로 물든다** — 테두리보다 눈에 잘 들어오고,
강도에 따라 농도가 달라져서 얼마나 걸렸는지도 보인다.

빈 칸 소환은 두 단계다 — 먼저 뭘 세울지 고르고, 고른 것 **하나의** 범위를 보여준 뒤
배치할지 다시 묻는다. 후보 셋의 원을 한꺼번에 겹쳐 그려봤더니 어느 원이 누구 건지
읽히지 않았다. 스택이 시간으로 쌓이므로 여전히 오래 머무는 적에 강하다.

| 3성 | 효과 | 대상 |
|---|---|---|
| A 분쇄파 | 오라 범위 +50% | 군집 |
| B 천공 | 범위 -15%, 스택당 -2.4, 상한 2배 | 정예·보스 |

| 5성 | 효과 |
|---|---|
| A1 파열 | 스택 최대 도달 시 스택 소모 → 물리 폭발 (스택수 x 5.0) |
| A2 전이 | 처치 시 스택 그대로 최근접 적에게 이관 |
| B1 파쇄 | 방어력 음수 허용 (-25까지). **모든 물리 타워가 혜택** |
| B2 관통 | 본인 공격만 방어력 완전 무시, 딜 +60% |

> B1 = 팀 플레이, B2 = 솔로 캐리. 저격/박격 많으면 B1, 파쇄자 단독이면 B2.

### 3.2 침식자 — 마법저항 감소 / 마법

```
base 0.8   공속 1.0s   사거리 2.7 오라형   마법
고유: 사거리 내 적 전체에
      마방 -(6+2x성급)%p          ← 마갑 카운터
      받는 마법딜 +(3+2x성급)%     ← 범용
      회복량 -50%                 ← 재생몹 카운터
```

마방 감소만 있으면 **마방이 0인 적(8종 중 4종)에게 아무 일도 안 일어난다.**
방어력은 5만 있어도 23%를 깎으므로 파쇄자는 항상 일을 하는데, 마방은 바닥이 0이라
침식자만 절반의 시간을 놀았다. 시뮬에서 침식자 기여도가 -3.5 ~ -9.6 으로 압도적 꼴찌였다.
감소는 마갑 카운터로 남기고, 범용 가치는 마법 취약으로 따로 준다.

파쇄자는 스택형(느림·단일 강함), 침식자는 오라형(즉시·개별 수치 약함). 역할 안 겹침.

| 3성 | 효과 | 대상 |
|---|---|---|
| A 확산 | 오라 범위 +50%, 효과 -30% | 군집·라인 전체 |
| B 심층 | 범위 -12%, 효과 2배 + 받는 마법딜 +15% | 정예·보스 |

| 5성 | 효과 |
|---|---|
| A1 역병 | 오라 이탈 후에도 3초 유지 → 경로 앞쪽에 미리 깔기 |
| A2 쇠약 | 오라 내 적 이속 -15% (슬로우 상한에 합산) |
| B1 붕괴 | 마방 음수 허용 (-30%p) |
| B2 소각 | 오라 내 적 **최대HP 1%/초 순수딜** → 고HP 탱커·보스 해답 |

### 3.3 서리탑 — 슬로우 / 마법

```
base 0.6   공속 0.9s   사거리 2.4   마법
오라: 사거리 안의 적 이속 -30% (상시)
장판: 사거리 안 전원에게 지속딜. 총량 고정이라 인원수로 나눠 들어간다
```

**딜도 장판이다.** 단일 빔이면 오라 타워인지 딜러인지 안 읽힌다.

다만 인원수만큼 곱해지면 군집에서 박격포 역할을 먹는다 — 그대로 재봤더니 서리탑
기여도가 +7.4, 박격포가 -5.2 였고 계수를 절반으로 줄여도 +8.8 로 오히려 올랐다.
문제는 딜 크기가 아니라 **적 수에 비례해 늘어나는 구조**였다.

동시 타격 수에 상한(4마리)을 걸어 봤지만 "범위 안인데 안 맞는 적"이 생기고,
누가 맞는지가 스폰 순서라 눈으로 못 읽었다. 지금은 **총량 고정 분배**다 —
범위 안 전원이 맞되 총 피해는 인원수와 무관하다.

**지속딜은 피격 플래시를 내지 않는다.** 매 프레임 때리므로 플래시를 내면
장판 안의 적이 계속 흰색이 되어 다른 상태 표시가 전부 묻힌다.

파쇄자와 같이 오라형이다. 경로의 어느 구간을 늦출지를 배치로 정한다.

**슬로우 총합 상한 60%** (모든 소스 합산). 성급업은 % 가 아니라 지속시간·범위만 증가.
CC는 항상 수치딜보다 세다. 상한 없으면 서리탑 도배가 정답 됨.

| 3성 | 효과 | 대상 |
|---|---|---|
| A 광역 냉각 | 오라 범위 +50% | 군집·질주 |
| B 결빙 | 범위 -15%, 슬로우 -55%, 오라에 2.5초 머물면 **1초 빙결** | 정예 |

빙결 = 완전정지. **면역몹에겐 무효.**

| 5성 | 효과 |
|---|---|
| A1 한파 | 슬로우 걸린 적이 받는 **모든 데미지 +12%** |
| A2 동상 | 슬로우 걸린 적 초당 순수딜 (재생몹 상쇄) |
| B1 연쇄 빙결 | 빙결 시 주변 2마리도 0.5초 빙결 |
| B2 취약 결정 | 빙결 해제 순간 물리·마법 취약 +25%, 3초 |

### 3.4 박격포 — 스플래시 / 물리

```
base 0.8   공속 1.6s   사거리 장   물리   착탄 지연 0.5s
```

**착탄 지연 때문에 질주몹에 약함. 이건 의도된 약점이고 고쳐주지 말 것.**

| 3성 | 효과 | 대상 |
|---|---|---|
| A 연발 | 공속 2배, 딜 55%, 반경 -20% | 군집 |
| B 중포 | 공속 -30%, 딜 180%, 반경 +40% | 정예·면역 |

| 5성 | 효과 |
|---|---|
| A1 탄막 | 1발 → 경로 위 3발 (발당 `CFG.BARRAGE_SHARE` = 0.6, 간격 `CFG.BARRAGE_GAP` = 1.2칸) |
| A2 소이 | 착탄 지점 장판 3초, 초당 딜 (경로 병목에 강함) |
| B1 충격파 | 착탄 시 0.3초 기절 — 면역몹 무효 |
| B2 철갑탄 | 방어력 50% 무시 + 정예·보스 딜 +40% |

#### 탄막은 「고를 이유가 없는 5성」이었다 — 원인은 분산이 아니라 딜 나눔

제보는 「서리탑 4 + 박격포 4성 도배로 면역 웨이브를 못 넘는다」였다. 스테이지4 ·
덱 `박격·서리·침식` · 12판 실측에서 A1 탄막만 w25 넘김 **4/12**, 같은 A 분기의
소이가 12/12 였다.

원인으로 먼저 지목된 것은 **이중 분산**이었다 — 3발이 `targets[0]/[1]/[2]` 로 서로
다른 적을 겨냥하는데 거기에 가로 ±0.6칸 지터를 한 번 더 얹고 있었다. **그런데
그게 아니었다.** 둘을 각각 걷어내고 재보면 결과가 안 움직인다:

| 후보 | w25 넘김(12판) |
|---|---|
| 원본 (이중 분산 + `dmg/3`) | 4/12 |
| 지터만 제거 | 4/12 |
| 3발을 한 점에 집중 (헛발 0) | 4/12 |
| 경로 위 3점 + `dmg/3` | 1/12 |
| 경로 위 3점 + 발당 0.45 | 6/12 |
| 경로 위 3점 + 발당 0.5 | 8/12 |
| **경로 위 3점 + 발당 0.6 (채택)** | **12/12** |
| 경로 위 3점 + 발당 0.5 + 반경 페널티 취소 | 12/12 (홀드아웃 20/24 로 더 낮음) |

진짜 원인은 `dmg / shots` 다. **발당 1/3 x 3발이면 탄막의 딜 총합이 무분기 1발과
정확히 같다.** 방어력이 곱연산(`applyArmor`)이라 쪼개도 손해가 없는 대신 이득도
없다 — 즉 탄막은 어떻게 뿌리든 무분기를 못 넘고, 한 발이라도 빈 칸에 떨어지면
그만큼 손해인 **상한 1.0배짜리 5성**이었다. 소이가 1발 풀딜 + 3초 장판(총 1.5발분)을
더 받는 것과 나란히 두면 격차가 그대로 설명된다. PM 표에서 소이와 DPS 가 똑같이
43 으로 보였던 건 3발을 전부 명중으로 세었기 때문이다.

그래서 고친 것은 뿌리는 방식이 아니라 **발당 계수**다. 0.6 x 3 = 1.8발분을 주되
3발을 경로 위 `-GAP / 0 / +GAP` 에 놓아 **줄지어 오는 직선을 덮게** 했다 — `how`
(「적이 줄지어 오는 직선을 덮는다」)가 원래 약속하던 그림이다. 간격 1.2 는 A 반경
0.64 의 지름 1.28 에 맞춘 값이라 세 폭발이 거의 안 겹치고 경로 2.6칸이 연속으로
덮인다. **줄이 꽉 차면 1.8배, 적이 하나뿐이면 0.6배** — 「군집에 강하고 단일에
약함」이 계수가 아니라 배치에서 나온다.

계수·간격은 손으로 안 맞췄다. 위 표로 고른 뒤 **시드를 갈아 끼운 홀드아웃 24판**
으로 순위를 확인했다(0.6 = 22/24, 0.5 = 18/24, 손 안 댄 원본 11/24, 소이 24/24).
간격도 0.9/1.2/1.5 를 쟀고 1.2 가 가장 높았다. 반경 −20% 를 A1 에서만 되돌리는
안은 홀드아웃에서 더 낮았고 A 분기가 약속한 페널티를 5성에서 몰래 취소하는
셈이라 버렸다.

**다른 세 분기는 안 움직인다.** `shots === 1` 인 경로(무분기·A2·B1·B2)는 코드가
그대로고, 12판 결과 문자열도 전후가 같다. 시드 고정 `parity` 도 A/A2·B/B1·B/B2 가
소수점까지 동일하고 A/A1 만 폭 4.22 → **3.20**, 박격 기여도 −0.86 → **+0.29** 로
움직였다.

`tools/seedcheck.js` 1번 케이스(`서리·박격·마력`, 기본 분기 A/A1)를 새로 떴다.
난수 호출 횟수도 144 → 155 로 같이 움직인 첫 사례라 근거를 그 파일 주석에 적었다 —
요약하면 **박격포 블록은 난수를 한 번도 안 뽑고**, 늘어난 11 회가 전부 소환
지점(138 → 149)이며 7성 특성 롤은 6 → 6 으로 그대로다. 판이 길어져 소환을 더 한
결과이지 난수 소비 규칙이 바뀐 게 아니다.

### 3.5 관측소 — 단일 고딜 / 물리

```
base 2.2   공속 2.0s   사거리 최장   물리
타겟팅: 표적 유지 — 직전 표적이 사거리 안에 살아 있으면 계속 노린다.
        새로 고를 때만 사거리 내 HP 최대
```

#### 매 발 HP 최고를 다시 고르면 연속타격이 원리적으로 안 쌓인다

원래는 발사할 때마다 `targets.sort((a, b) => b.hp - a.hp)` 로 1위를 다시 뽑았다.
**자기가 방금 깎은 놈이 그만큼 내려가 다음 발에는 다른 놈이 1위가 된다.** 표적이
갈리면 `t.lastTarget !== main.id` 라 `streak` 이 1 로 리셋되므로, 연속타격은
관측소가 잘 못해서가 아니라 **타겟팅 규칙 때문에 구조적으로 못 쌓였다.**

스테이지 4 · 덱 `관측·서리·침식` · 6판 · 표본 42만건 실측(`streak` 매 프레임 샘플링,
딜 보너스와 같게 5 에서 자름):

| | HP 최고 재선택 | 표적 유지 |
|---|---|---|
| 평균 `streak` | 1.31 | **2.29** |
| 5연타 도달 | 4.6% | **20.5%** |
| 속사 딜 보너스 (설계 최대 +40%) | +10.5% | +18.3% |

A1 표식의 「5연타 시 모든 타워 딜 +20%」가 시간의 4.6% 만 켜져 있었다. 그래서 단일
DPS 는 5성 일격 355.8 대 박격포 중포 203.8 로 전 분기 우위인데 기여도(§2 대등성)는
반대로 나왔다 — A/A1 −1.51 로 7종 중 혼자 음수였다.

**고친 것은 계수가 아니라 조건이다.** `streak` 계수를 8% → 32% 로 올려 평균을 맞추는
길도 있었지만, 그러면 4.6% 확률로 터지는 5연타가 +160% 가 되어 분산만 커진다.
`STAR_MULT` · `CFG.DMG_SCALE` · `base`/`cd`/`range` · 분기 배수는 한 줄도 안 건드렸다.

재선택 조건은 셋뿐이고 전부 `enemiesInRange()` 한 곳에서 나온다 — 표적이 죽거나,
사거리를 벗어나거나, 사거리가 비거나. `targets.find(e => e.id === t.lastTarget)` 가
`undefined` 를 내면 그때 다시 HP 최고를 고른다. **바뀐 것은 "무엇을 고르는가"가
아니라 "언제 다시 고르는가"다.**

기여도 재측정(56덱 x 4시행, 시드 12345):

| 분기 | 관측소 전 → 후 |
|---|---|
| A/A1 | −1.51 → **+1.64** |
| A/A2 | +0.40 → **+1.42** |
| B/B1 | +2.25 → +2.37 |
| B/B2 | +1.13 → +0.85 |

**B 분기(일격)에도 같이 걸린다** — `streak` 은 A 딜에만 붙지만 표적 선택은 공용이다.
B1 처형(HP 18% 이하 즉사)은 표적이 즉사하는 순간 `e.dead` 로 목록에서 빠져 자동
재조준되므로 유지가 끊기고, B2 관통사격은 **`main` 을 피해 목록의 첫 칸에 강제로
넣도록 같이 고쳤다.** HP 정렬 상위 4개를 그대로 쓰면 표적을 유지하는 동안 `main` 이
5위 밖으로 밀려 **총구·빔이 겨눈 적만 피해 목록에서 빠지는** 그림이 나온다.

`tools/seedcheck.js` 의 0번 케이스 기대값(`life` 3 → 14)을 새로 떴다. 표적이 갈리는
시점이 달라지면 그 뒤 판정이 통째로 밀리므로 결과가 바뀌는 게 정상이다 — 의도한
밸런스 변경의 결과이지 회귀가 아니다. 난수 호출 횟수(102)는 그대로다.

| 3성 | 효과 | 대상 |
|---|---|---|
| A 속사 | 공속 +80%, 딜 60%, 같은 대상 연속타격 시 딜 +8% 누적(최대 5스택) | 범용·보스 |
| B 일격 | 공속 -40%, 딜 250%, 치명 25% / x2 | 정예·면역 |

| 5성 | 효과 |
|---|---|
| A1 표식 | 5회 연속 타격 시 대상에 표식 → **모든 타워 딜 +20%** |
| A2 점사 | 대상 처치 시 즉시 재공격 (연쇄로 군집도 처리) |
| B1 처형 | HP 18% 이하 즉사 (보스 제외) |
| B2 관통사격 | 직선 뒤쪽 3마리 관통 |

### 3.6 마력로 — 관통 라인 / 마법

```
base 0.7   충전 2.5s 후 발사   직선 관통   마법
빔 반폭 0.7칸. 대상을 향해 회전한다
```

**발사 판정은 사거리 원이 아니라 빔 직선이다.** 사거리 안의 적 하나하나를 향해
쏴 보고 **가장 많이 꿰는 방향**을 고른다. 선 위에 아무도 없으면 충전만 유지한 채 기다린다.

처음에는 각도를 배치 시점에 고정했다 — "배치 각도가 곧 성능"이라는 설계였다.
실제로 만져 보니 **"겨냥한 놈한테 안 쏜다"로만 읽혔다.** 원 안에 적이 있는데 선 밖이면
허공에 쏘는 것처럼 보였고, 조준각을 경로 기준으로 잡아도 90도 단위로 꺾이는 것처럼 보였다.
회전하게 바꾸고 관통만 남겼다. 배치는 여전히 중요하다 — 한 줄에 여러 마리가 서는
위치라야 관통이 값을 한다.

| 3성 | 효과 | 대상 |
|---|---|---|
| A 속사 코일 | 충전 1.2초, 딜 50% | 질주·군집 |
| B 과충전 | 충전 4초, 딜 240%, 관통 무제한 | 라인 밀집 |

| 5성 | 효과 |
|---|---|
| A1 연쇄 | 명중한 적에서 옆으로 3회 튐 |
| A2 공명 | 같은 적 연속 명중 시 마방 -5%p 누적 (침식자와 곱시너지) |
| B1 소멸 | 처치 수만큼 다음 발사 딜 +15% (최대 +150%) |
| B2 순수 광선 | 딜 타입 **순수**로 변경, 딜 65% → 면역몹 최종 해답 |

### 3.7 조폐소 — 경제 / 딜 0

```
공격 안 함. 5성도 1칸 유지
기본: 웨이브 종료 시 +12 x 성급계수 골드 (5성 = +398)
```

수입이 성급 계수를 그대로 타야 한다. 딜러와 같은 덱 슬롯, 같은 합성 비용을 두고
경쟁하기 때문이다. 고정값으로 뒀더니 시뮬에서 조폐소가 든 덱이 전부 하위권이었다.

| 3성 | 효과 |
|---|---|
| A 이자 | 보유 골드의 3% 추가 지급 (**상한 40골드**) |
| B 징수 | 사거리 내 적 처치 시 +성급 골드 |

| 5성 | 효과 |
|---|---|
| A1 투자 | 웨이브 클리어 골드 +25% |
| A2 환급 | 판매 100% 환급 + 합성 비용 무료 |
| B1 현상금 | 정예·보스 처치 시 +50골드 |
| B2 약탈 | 징수 시 25% 확률로 골드 대신 **정수** 획득 |

---

## 4. 7성 공통 특성 (랜덤 3개 제시 → 1택)

| 특성 | 효과 |
|---|---|
| 각인 | 인접 4칸 타워 데미지 +25% |
| 연산 과부하 | 자기 공속 +50%, 사거리 -25% |
| 결정화 | 웨이브 사이 3성/5성 분기 무료 재선택 |
| 징수관 | 이 타워 처치 시 +2골드 |
| 처형 프로토콜 | HP 12% 이하 즉사 (보스 제외) |
| 불굴 | 면역몹에게도 디버프 30% 적용 (레어, 출현율 낮게) |
| 공명 코어 | 같은 종류 타워 1개당 딜 +8% |

---

## 5. 적

| 몹 | 특징 |
|---|---|
| 중갑 | 방어력 40, HP 낮음 |
| 마갑 | 마방 높음 |
| 질주 | 이속 2.5배 |
| 재생 | 초당 HP 3% 회복 |
| **면역** | 방깎·마방깎·슬로우·기절 전부 무효 |
| 군집 | 20마리 소량 HP |
| 정예 | 1마리 거대 HP (기본 HP x6) |

### 커버리지 검증

밸런스의 핵심 — **모든 몹에 해답 3개 이상, 단일 타워로 전부 커버 불가.**

| 몹 | 해답 |
|---|---|
| 중갑 | 파쇄자 전 분기 · 박격-철갑탄 · 마력로-순수광선 |
| 마갑 | 침식자 전 분기 · 마력로-공명/순수광선 · 관측소(물리라 무시) |
| 질주 | 서리탑 전 분기 · 관측소(사거리) · 마력로-속사코일 |
| 재생 | 침식자 기본(회복-50%) · 서리탑-동상 · 관측소-일격 |
| 면역 | 관측소-일격 · 박격-중포 · 마력로-순수광선 · 7성 불굴 |
| 군집 | 박격-연발/탄막 · 서리탑-광역 · 파쇄자-분쇄파 · 관측소-점사 · 마력로-연쇄 |
| 정예 | 관측소-일격/처형 · 파쇄자-천공 · 박격-철갑탄 · 침식자-소각 |

**면역몹이 안전판.** 서포트 몰빵을 벌하고, 순수딜 분기(마력로 B2, 관측소 B)의 존재 이유를 만듦. 후반 웨이브의 면역몹 비율로 난이도 조절.

### 웨이브 편성

편성(어떤 몹이 언제 나오는지)은 커버리지 매트릭스에 맞춰 손으로 짜고,
**수량은 총 HP가 목표 곡선을 따르도록 역산한다** — `700 x 1.239^(w-1)`, 보스 웨이브만 x1.3.

수량을 손으로 정했더니 곡선이 톱니였다. 직전 웨이브 대비 x0.32 → x4.23 처럼
흔들려서, 실제 난이도가 편성이 아니라 그때그때의 총 HP에 좌우됐다.

**보스 웨이브는 한 가지 어려움만 시험한다.** w20 을 정예x3 + 면역x6 으로 뒀더니
HP 벽과 디버프 차단이 겹쳐서 20판 중 8판이 정확히 거기서 끝났다 — 보스가 아니라 벽이다.
지금은 w10/w20 이 정예(HP 벽), w30 이 정예+면역+재생(총정리)이다.

---

## 6. 밸런스 주의점

1. **증폭 3중첩** — 서리탑-한파(A1) + 관측소-표식(A2) + 파쇄자-파쇄(B1)이 곱연산이면 터짐.
   → 증폭 계열끼리는 **가산 합연산**, 방어력 감소만 별도 계산.
2. **조폐소 스노우볼** — 이자(A) + 투자(A1)가 초반에 터지면 후반 무한 자원.
   → 이자 상한 필수. 초반 웨이브를 조폐소 없이도 뚫리게 두지 말 것(기회비용 유지).
3. **7성 각인 도배** — 각인끼리 중첩되면 7성 벽 세우기 시작.
   → 각인은 **타워당 1개만 적용**(최고값).

---

## 7. 안 하기로 한 것

- **속성 상성표** (불/전기/냉기…) — "이번 판 불몹 → 물 찍어" 는 선택이 아니라 퀴즈. 역할 기반 디버프로 대체함.
- **미로 만들기(mazing)** — 모바일 터치에서 답답함. 경로는 고정, 배치 퍼즐로 대체.
- **성급 무한** — 숫자만 커지고 인플레 터짐. 7성 상한.
- **웨이브 60+** — 모바일 세션 길이에 안 맞음. 30 고정.
- **실시간 PvP** — 솔로 개발 + 서버는 함정.
- **타워 개별 스탯창 마이크로 관리** — 터치에서 못 씀.
- **디버프 비중첩** — 방깎 1개면 충분해지면 합성 선택이 죽음. 그래서 스택형 + 자체 딜러로 설계함.
