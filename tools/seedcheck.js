// 시드 고정 밸런스 회귀.
//
//   node tools/seedcheck.js
//
// tools/test.js 는 전역 Math.random 을 그대로 두므로 실행할 때마다 결과가 다르다.
// 그래서 "밸런스를 한 줄도 안 고쳤는데 시뮬 결과가 바뀌었다"를 잡아내는 수단이
// 이 파일뿐이다. 연출·이펙트처럼 게임 규칙과 무관한 작업일수록 여기가 유일한 감시다.
// 별도 명령이면 아무도 안 돌리므로 `npm test` 가 test.js 다음에 이걸 이어서 돌린다.
//
// 결과 문자열만 보는 게 아니라 Math.random 호출 횟수까지 센다. 난수를 한 번 더
// 뽑으면 그 뒤의 모든 판정이 한 칸씩 밀린다 — 관측소 치명(25%), 조폐소 약탈(25%),
// 소환 자리, 덱·특성 롤이 전부 같은 스트림을 쓰기 때문이다. 이건 결과가 우연히
// 같아도 잠재적 회귀라서, 횟수가 어긋나면 실패로 본다.
//
// 연출 코드가 난수를 써야 하면 전역 Math.random 말고 연출 전용 시드를 따로 둘 것.
const { load, greedy } = require('./sim.js');

// verify-build.mjs 와 같은 LCG. 두 하네스가 같은 시드를 쓰면 스크린샷과
// 이 표가 같은 판을 가리킨다.
const SEED = 12345;

function seedRandom() {
  const orig = Math.random;
  let s = SEED >>> 0;
  let n = 0;
  Math.random = () => { n++; s = (s * 1664525 + 1013904223) >>> 0; return s / 2 ** 32; };
  return { calls: () => n, restore: () => { Math.random = orig; } };
}

// 기대값은 base 커밋에서 실측한 것이다. 밸런스를 의도적으로 바꿨다면
// 이 표를 같이 고쳐야 하고, 안 바꿨는데 어긋나면 그게 회귀다.
//
// [2026-08 관측소 표적 유지] 0번 케이스의 life 를 3 → 14 로 새로 떴다.
// 관측소 발사부가 매 발 HP 최고를 다시 고르던 것을 "직전 표적이 사거리 안에 살아
// 있으면 유지"로 바꿨다(index.html fireTower). 표적이 갈리는 시점이 달라지면 그
// 뒤의 처치 순서가 통째로 밀리므로 결과가 바뀌는 게 정상이다 — **의도한 밸런스
// 변경의 결과이지 회귀가 아니다.** 통과시키려고 맞춘 숫자와 구분되는 근거는 셋이다.
//   ① 난수 호출 횟수 102 가 그대로다. 관측소는 A 계열에서 난수를 안 뽑으므로
//      스트림이 밀렸다면 그건 이 변경이 아니라 다른 것이다
//   ② 바뀐 방향이 예측과 같다. 관측소가 든 덱이라 딜이 올라 관문이 덜 샜다(life↑).
//      1·3번 케이스는 문자열까지 그대로다 — 1번은 덱에 관측소가 없고, 3번은
//      관측소가 7성까지 못 가서 표적 선택이 결과를 못 바꾼다
//   ③ 연속타격 실측이 평균 1.31 → 2.29(최대 5), 5연타 도달 4.6% → 20.5% 로 움직였고
//      그게 tools/test.js 「관측소 표적 유지」에 단언으로 박혀 있다
//
// [2026-08 파쇄자 물리 취약] 같은 티켓의 두 번째 변경(`CFG.SHRED_VULN`)까지 반영해
// 세 케이스를 **다시 떴고, 세 줄 다 위 값 그대로였다.** 0번 케이스는 덱에 파쇄자가
// 있는데도 안 움직였는데, 그리디가 이 판을 20웨이브 클리어로 끝내서 요약 여섯 칸
// (result/wave/life/towers/maxStar/gold)이 전부 상한에 걸려 있기 때문이다 — 딜이
// 올라도 더 올라갈 칸이 없다. 기여도(parity)는 파쇄 −4.32 → −0.12 로 움직였으므로
// **"안 바뀌었으니 안 걸린 것"이 아니라 "이 세 케이스가 그 차이를 못 보는 것"이다.**
// 파쇄자 쪽 회귀를 잡는 것은 tools/test.js 「파쇄자 물리 취약」과 「기여도 폭」이다.
// [2026-08 박격포 탄막] 1번 케이스를 통째로 새로 떴다(over w24 → clear w25, rand 144 → 155).
// 탄막(A1)이 발당 `dmg / 3` 이라 3발을 다 맞혀도 딜 총합이 무분기 1발과 같았던 것을
// `CFG.BARRAGE_SHARE`(0.6 x 3발 = 1.8발분) + 경로 위 3점 착탄으로 바꿨다(index.html
// fireTower). 이 케이스의 덱이 `서리·박격·마력` 이고 greedy 의 기본 분기가 A/A1 이라
// **정확히 이 변경을 정면으로 맞는 유일한 케이스**다. 0·3번은 덱에 박격포가 없어
// 문자열까지 그대로다.
//
// 난수 호출 횟수가 처음으로 같이 움직였다. 위 관측소 건은 102 가 그대로인 것이 근거
// ① 이었는데 여기서는 못 쓰므로, 대신 **호출 지점별로 갈라서** 근거를 만들었다:
//   ① 박격포 블록은 난수를 한 번도 안 뽑는다. 전역 Math.random 호출부는 여섯 곳뿐이고
//      (rollDeck · summon 종류 · summon 자리 · 7성 특성 롤 · 조폐 약탈 · 관측 치명)
//      이 덱에는 조폐도 관측도 없다. 즉 스트림이 밀린 건 박격포가 난수를 더 써서가
//      **아니다**
//   ② 실제로 갈라 세어 보니 +11 이 전부 **소환** 지점이고(138 → 149), 7성 특성 롤은
//      6 → 6 으로 그대로다. 판이 길어지고(w24 사망 → w25 클리어) 골드가 14 → 1018 로
//      남아 소환을 11번 더 한 결과이지, 난수 소비 규칙이 바뀐 게 아니다
//   ③ 바뀐 방향이 예측과 같다. 박격포 딜이 올라 관문이 덜 샜다(life 0 → 6, 클리어).
//      towers 가 4 → 5 로 는 것도 같은 원인(골드가 남아 한 대 더 세웠다)이다
//   ④ 계수 0.6 은 이 케이스를 통과시키려고 고른 값이 아니다. 스테이지4 · 12판에서
//      0.45/0.5/0.6 을 재고 고른 뒤 홀드아웃 시드 24판으로 순위를 확인했으며,
//      그 측정이 tools/test.js 「박격포 탄막」에 단언으로 박혀 있다
//
// [2026-08 셋째 종류 (#31)] **세 케이스를 전부 새로 떴다.** 바뀐 것은 index.html 이
// 아니라 `tools/sim.js` 의 `pickKind` 다 — 한 대도 없는 종류가 `Infinity` 폴백으로
// 항상 꼴찌가 되어 그리디가 덱의 셋째 종류를 아예 안 짓고 있었다. 여기 세 케이스도
// 전부 덱 3종 중 2종만 등장했었다(0번 mint 없음 · 1번 arc 없음 · 3번 marksman 없음).
// **이제 세 케이스 다 towers 에 덱 3종이 전부 나온다** — 그게 이 티켓의 직접 지표다.
//
// 난수 호출 횟수가 세 줄 다 움직였다. 위 박격포 건과 같은 방식으로 **호출 지점별로
// 갈라서** 근거를 만들었다. 전역 Math.random 호출부는 여섯 곳뿐이고(rollDeck ·
// summon 종류 · summon 자리 · 7성 특성 롤 · 조폐 약탈 · 관측 치명), greedy 는 덱과
// 소환 종류를 항상 명시로 넘기므로 앞의 둘은 세 케이스 전부 0 이다.
//
//   케이스   소환 자리      7성 특성 롤   관측 치명     합계
//     0      99 → 196       3 → 9        0 → 0        102 → 205
//     1     149 → 128       6 → 3        0 → 0        155 → 131
//     3     138 → 190       6 → 6        0 → 399      144 → 595
//
//   ① 0번: mint(조폐소)가 처음으로 지어진다. 경제 타워라 골드가 575 → 3120 으로
//      늘고 그만큼 소환을 더 한다(+97). 방향이 예측과 같다 — 딜이 늘어 관문이 덜
//      샜다(life 14 → 20). 여기서 늘어난 난수는 전부 **소환 자리와 특성 롤**이고,
//      조폐 약탈(25%)은 b5 === 'B2' 조건이라 greedy 기본 분기(A/A1)에서 안 돈다
//   ② 3번: marksman(관측소)이 처음으로 지어지고 이 케이스만 분기가 B 다. 관측 치명
//      (`t.b3 === 'B' && Math.random() < 0.25`)이 **발당 한 번** 도는 자리라 0 → 399
//      가 전부 거기서 나왔다. 즉 스트림이 밀린 건 난수 소비 규칙이 바뀌어서가 아니라
//      **난수를 쓰는 타워가 처음으로 판에 존재하게 되어서**다. 방향도 예측과 같다
//      (딜이 늘어 w24 → w28 로 더 버텼다)
//   ③ 1번은 **나빠졌다**(clear w25 → over w23). 이것도 예측 안이다 — 같은 골드를
//      세 종류로 나누면 종류당 개수가 줄어 성급이 늦게 오른다. 실제로 타워가
//      frost1/3/5/7 + mortar7 (5대) 에서 frost7 + mortar6 + arc6 (3대)로 바뀌었고,
//      박격포가 7 → 6 으로 내려간 만큼(계수 191 → 79.6) 딜이 줄었다. 판이 짧아져
//      소환도 21회 덜 한다(149 → 128). **회귀가 아니라 3종을 실제로 짓게 된 결과**이며,
//      난이도 재조정은 별도 티켓이다(index.html 은 이 티켓에서 한 줄도 안 고쳤다)
// [2026-08 배치 정책 (#35)] **세 케이스를 전부 새로 떴다.** 여기서도 바뀐 것은
// `index.html` 이 아니라 `tools/sim.js` 다 — 그리디가 소환 자리를 균등 난수로 고르던
// 것을 **k-표본 최고**로 바꿨다(`SUMMON_SAMPLES = 2`: 후보 두 곳을 뽑아 커버가 큰
// 쪽). 9x14 보드에서 초반 두세 대가 통째로 헛자리에 서서 판이 무너지던 꼬리를
// 없애려는 것이고, DESIGN §꼬리를 재는 자에 근거가 있다.
//
// **이 티켓은 두 커밋이고, 앞 커밋은 이 표를 한 글자도 안 움직였다.** 자리 고르기를
// sim 쪽으로 옮기기만 한 배관 커밋은 난수 소비 지점·횟수·인덱스가 전부 같아서
// 205 / 131 / 595 가 그대로 나왔다. 아래 값이 달라진 것은 **정책 커밋 하나** 때문이다.
//
// 난수 호출 횟수가 세 줄 다 움직였다. 박격포·#31 건과 같은 방식으로 **호출 지점별로
// 갈라서** 근거를 만들었다. 전역 Math.random 호출부는 여섯 곳이고(rollDeck ·
// summon 종류 · summon 자리 · 7성 특성 롤 · 조폐 약탈 · 관측 치명), greedy 는 덱과
// 소환 종류를 항상 명시로 넘기므로 앞의 둘은 세 케이스 전부 0 이다. **소환 자리는
// 이제 index.html 이 아니라 sim.js 가 뽑는다**(index.html 의 무좌표 분기는 그대로
// 남아 있고 shot.js·test.js 가 여전히 쓴다).
//
//   케이스   소환 자리        7성 특성 롤   관측 치명      합계        소환 횟수
//     0      196 → 392        9 → 9        0 → 0        205 → 401    196 → 196
//     1      128 → 302        3 → 3        0 → 0        131 → 305    128 → 151
//     3      190 → 378        6 → 6      399 → 350      595 → 734    190 → 189
//
//   ① **늘어난 난수는 전부 「소환 자리」이고, 정확히 소환 횟수의 2배다.** 0번은
//      392 = 2 x 196 으로 **소환 횟수가 한 번도 안 변했는데** 자리 난수만 정확히
//      두 배가 됐다 — 소비 규칙이 `1 → k` 로 바뀐 것 말고는 아무것도 안 달라졌다는
//      가장 깨끗한 증거다. 1·3번도 302 = 2 x 151, 378 = 2 x 189 로 전부 짝수다
//   ② 7성 특성 롤은 세 줄 다 그대로다(9 / 3 / 6). 배치는 특성 롤 횟수를 안 건드린다
//   ③ 3번의 관측 치명이 399 → 350 으로 **줄었다.** 관측소가 발당 한 번 뽑는
//      자리라 발사 횟수에 비례하는데, 타워가 좋은 자리로 가면서 표적이 더 빨리 죽어
//      같은 웨이브를 더 적은 발수로 넘긴다. 방향이 예측과 같다
//   ④ 방향도 예측 안이다. 1번은 `over w23 → over w25` 로 **두 웨이브 더 버틴다**
//      (박격포가 6성 하나에서 1/2/3/5/6 다섯 대로 흩어졌는데도 총딜이 늘었다).
//      3번은 w28 로 같지만 남은 골드가 90 → 22 로 줄어 더 많이 지었다.
//      0번은 20웨이브 클리어라 요약 여섯 칸이 이미 상한에 걸려 있어 life 가
//      20 → 19 로 한 칸 움직인 게 전부다 — 파쇄자 건과 같은 이유로 **이 케이스는
//      배치 개선을 거의 못 본다**
//   ⑤ **「좋아졌으니 맞다」는 근거로 안 쓴다.** 난수 소비량이 `1 → k` 로 바뀌어
//      첫 소환부터 스트림이 갈리므로 #31 의 「시드 10개 짝지어 부호 일치」는 여기서
//      못 쓴다. 진짜 근거는 위 ①(퇴화값 `k=1` 이면 205/131/595 가 비트 단위로
//      돌아온다 — `tools/test.js` 「배치 백분위」가 그 성질을 잠근다)과, 난이도와
//      독립인 **백분위 지표**(`node tools/place.js`)다
// [2026-08 종류별 자리 점수 (#48)] **1·3번만 새로 떴다. 0번은 한 글자도 안 움직였다.**
// 여기서도 바뀐 것은 `index.html` 이 아니라 `tools/sim.js` 다 — 그리디가 일곱 종을
// 커버 하나로 놓던 것을 박격포는 「폭발이 덮는 경로」, 마력로는 「빔이 꿰는 경로」로
// 바꿨다(나머지 다섯은 커버 그대로). 근거는 `npm run affinity` 이고 DESIGN
// §판이 어느 타워를 좋아하는가에 있다.
//
// **0번이 안 움직인 것이 이 티켓의 범위 증거다.** 덱이 `파쇄·관측·조폐` 라 자를 바꾼
// 두 종류가 아예 없고, 그래서 결과 문자열도 rand 401 도 그대로다. 자가 다른 종류로
// 샜다면 여기가 먼저 걸린다.
//
// 난수 호출 횟수를 **호출 지점별로 갈라 센 표**(위 #35 건과 같은 방식):
//
//   케이스   소환 자리      7성 특성 롤   관측 치명      합계        소환 횟수
//     0      392 → 392     9 → 9        0 → 0        401 → 401    196 → 196
//     1      302 → 278     3 → 3        0 → 0        305 → 281    151 → 139
//     3      378 → 350     6 → 6      350 → 267      734 → 623    189 → 175
//
//   ① **줄어든 난수는 전부 「소환 자리」와 그에 딸린 것이고, 여전히 소환 횟수의 정확히
//      2배다**(278 = 2 x 139 · 350 = 2 x 175). 소비 규칙(`k` 회)은 안 건드렸고 판이
//      짧아져 소환을 덜 한 것이다 — 규칙이 바뀌었다면 짝수가 안 나온다
//   ② 7성 특성 롤은 두 줄 다 그대로다(3 / 6). 자리 고르기는 특성 롤을 안 건드린다
//   ③ 3번의 관측 치명이 350 → 267 로 줄었다. 발당 한 번 도는 자리라 판이 w28 → w27 로
//      짧아진 만큼 발수가 준 것이다. 방향이 예측과 같다
//   ④ **두 케이스 다 나빠졌다 — 숨기지 않는다.** 1번 `over w25 → w24` · 3번
//      `over w28 → w27`. 시드 하나짜리 판이라 이걸로 정책의 좋고 나쁨을 판정하지
//      않는다(같은 이유로 #35 도 1번이 나빠진 채 갔다). 정책을 재는 자는 210판짜리
//      `npm run curve` 와 35덱짜리 `npm run affinity` 이고, 거기서는 곡선이 거의 안
//      움직였다(진도 p 여섯 판 전부 ±0.01 안). **「좋아졌으니 맞다」도 「나빠졌으니
//      틀렸다」도 이 세 줄로는 말할 수 없다**
//   ⑤ 진짜 근거는 아래 퇴화값 블록이다 — `k=1` 로 두면 세 줄이 **비트 단위로**
//      돌아온다. `k=1` 에서는 뽑은 인덱스를 그대로 쓰므로 점수 함수가 무엇이든 결과가
//      같아야 하고, 실제로 205/131/595 가 그대로다. 즉 움직인 것은 **자 하나뿐**이다
const CASES = [
  {
    name: '0 / 파쇄·관측·조폐',
    opts: { stage: 0, deck: ['shredder', 'marksman', 'mint'] },
    expect: '{"result":"clear","wave":20,"life":19,"towers":["marksman7","mint7","shredder3","shredder7"],"maxStar":7,"gold":3120}',
    rand: 401,
  },
  {
    name: '1 / 서리·박격·마력',
    opts: { stage: 1, deck: ['frost', 'mortar', 'arc'] },
    expect: '{"result":"over","wave":24,"life":0,"towers":["arc6","frost7","mortar1","mortar2","mortar4","mortar6"],"maxStar":7,"gold":64}',
    rand: 281,
  },
  {
    name: '3 / 침식·마력·관측 (B,B1)',
    opts: { stage: 3, deck: ['eroder', 'arc', 'marksman'], branch3: 'B', branch5: 'B1' },
    expect: '{"result":"over","wave":27,"life":0,"towers":["arc7","eroder7","marksman1","marksman2","marksman3","marksman4","marksman6"],"maxStar":7,"gold":72}',
    rand: 623,
  },
];

let fail = 0;
for (const c of CASES) {
  const rng = seedRandom();
  let got, calls;
  try {
    got = JSON.stringify(greedy(load(), c.opts));
    calls = rng.calls();
  } finally {
    rng.restore();
  }

  const sameResult = got === c.expect;
  const sameCalls = calls === c.rand;
  if (!sameResult || !sameCalls) fail++;

  console.log((sameResult && sameCalls ? '  PASS ' : '  FAIL ') + c.name +
    '   rand ' + calls + (sameCalls ? '' : ' (기대 ' + c.rand + ')'));
  if (!sameResult) {
    console.log('    기대 ' + c.expect);
    console.log('    실제 ' + got);
  }
}

// ── 퇴화값 동일성 (#35) ──────────────────────────────────────
// `SUMMON_SAMPLES = 1` 이면 k-표본 최고는 「후보에서 난수로 한 곳 뽑기」와 **같은
// 식**이 된다. 그러면 난수 소비 지점·횟수·인덱스가 배치 정책을 붙이기 전과 전부
// 같으므로 결과가 **비트 단위로 같아야** 한다.
//
// **이게 이 정책의 유일한 강한 근거다.** 난수 소비량이 `1 → k` 로 바뀌면 첫 소환부터
// 스트림이 갈려서, #31 이 쓴 「시드 10개를 짝지어 부호가 일치하는가」를 여기서는 못
// 쓴다(밀린 것과 좋아진 것을 못 가른다). 대신 **노브를 퇴화값으로 되돌리면 옛 값이
// 정확히 돌아온다**는 것이 「바뀐 것은 정책 하나뿐」임을 보인다.
//
// 아래 값은 #35 이전(균등 난수)의 실측이고 이 파일이 오래 들고 있던 표 그대로다.
// **위 CASES 를 새로 뜰 일이 있어도 이 세 줄은 안 바뀐다** — 바뀐다면 그건 배치
// 정책 말고 다른 것이 같이 움직였다는 뜻이라 그때는 진짜 회귀다.
const DEGENERATE = [
  '{"result":"clear","wave":20,"life":20,"towers":["marksman7","mint7","shredder3","shredder7"],"maxStar":7,"gold":3120}',
  '{"result":"over","wave":23,"life":0,"towers":["arc6","frost7","mortar6"],"maxStar":7,"gold":4}',
  '{"result":"over","wave":28,"life":0,"towers":["arc7","eroder7","marksman2","marksman3","marksman4","marksman5","marksman6"],"maxStar":7,"gold":90}',
];
const DEGENERATE_RAND = [205, 131, 595];

console.log('\n  ── 노브를 퇴화값(SUMMON_SAMPLES = 1)으로 두면 #35 이전과 같아야 한다 ──');
CASES.forEach((c, i) => {
  const rng = seedRandom();
  let got, calls;
  try {
    got = JSON.stringify(greedy(load(), { ...c.opts, samples: 1 }));
    calls = rng.calls();
  } finally {
    rng.restore();
  }
  const same = got === DEGENERATE[i] && calls === DEGENERATE_RAND[i];
  if (!same) fail++;
  console.log((same ? '  PASS ' : '  FAIL ') + c.name +
    '   rand ' + calls + (calls === DEGENERATE_RAND[i] ? '' : ' (기대 ' + DEGENERATE_RAND[i] + ')'));
  if (got !== DEGENERATE[i]) {
    console.log('    기대 ' + DEGENERATE[i]);
    console.log('    실제 ' + got);
  }
});

console.log(fail ? `\n시드 회귀 실패 ${fail}건` : '\n시드 회귀 전부 통과');
process.exit(fail ? 1 : 0);
