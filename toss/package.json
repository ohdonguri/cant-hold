{
  "name": "cant-hold-toss",
  "version": "1.0.0",
  "private": true,
  "description": "이번 웨이브는 못 막습니다 — 앱인토스 미니앱판. 게임은 웹판(../index.html) 한 벌뿐이고 sync.mjs 가 떠 온다.",
  "type": "module",
  "scripts": {
    "dev": "node sync.mjs && vite dev",
    "build": "node sync.mjs && vite build",
    "preview": "vite preview",
    "pack": "npm run build && ait build",
    "deploy": "npm run build && ait deploy",
    "sync": "node sync.mjs",
    "sync:check": "node sync.mjs --check"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^3.0.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.2.0"
  }
}
